{
  "name": "mozhou",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "prepare": "node scripts/install-git-hooks.mjs",
    "setup:git-hooks": "node scripts/install-git-hooks.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:web": "pnpm --filter @mozhou/web dev",
    "dev:api": "node scripts/dev.mjs --api-only",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "desktop:dev": "pnpm run build:sidecar && pnpm --filter @mozhou/desktop tauri dev",
    "desktop:build": "pnpm run build:sidecar && node scripts/build-desktop.mjs",
    "desktop:check": "pnpm run build:sidecar && cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "release:metadata": "node scripts/release-metadata.mjs",
    "release:preflight": "node scripts/release-preflight.mjs",
    "backend:sync": "uv sync --project services/api",
    "lint": "pnpm --filter @mozhou/web lint && uv run --project services/api --no-sync ruff check services/api",
    "typecheck": "pnpm --filter @mozhou/web typecheck && uv run --project services/api --no-sync mypy services/api/app",
    "test": "pnpm --filter @mozhou/web test",
    "test:scripts": "node --test scripts/*.test.mjs",
    "guard:repository": "node scripts/repository-guard.mjs",
    "test:backend": "uv run --project services/api --no-sync pytest services/api/tests",
    "benchmark:scale": "uv run --project services/api --no-sync python scripts/performance-benchmark.py",
    "build": "pnpm --filter @mozhou/web build",
    "verify": "pnpm run guard:repository && pnpm run lint && pnpm run typecheck && pnpm run test:scripts && pnpm run test && pnpm run test:backend && pnpm run benchmark:scale && pnpm run build && pnpm run desktop:check"
  }
}
