# 墨舟安全说明

## 支持范围

安全修复以最新的 `main` 和最新发布候选为准。尚未通过 `PLAN.md` 发布门的构建只用于封闭测试，不应被描述为正式版。

## 报告安全问题

请使用 GitHub 的 [Private vulnerability reporting](https://github.com/shenxiaofeng-pro/mozhou/security/advisories/new) 提交，不要在公开 Issue 中粘贴 API Key、作者正文、参考原文、本地数据库、诊断包或可复现的利用细节。

报告应尽量只包含：受影响版本、操作系统、无隐私数据的复现步骤、影响和建议缓解方式。墨舟的脱敏诊断包可由作品书架的“系统诊断”导出；它默认不包含创作内容和凭据。

## 安全边界

- 桌面 API 只监听 `127.0.0.1`，每次启动使用新的 256 位会话令牌。
- API Key 由系统密钥库保存，不写入 SQLite、日志、归档或诊断包。
- AI 调用前必须展示外发范围并由作者确认；未经批准的 AI 结果不能写入正文或正式事实。
- 参考原文默认不进入项目归档，完整资料归档必须由作者单独选择。
- 发布标签必须通过依赖审计、限制性许可证门、签名/公证、SBOM、校验和和来源证明。

公开仓库不等于项目已采用开源许可证。除第三方组件各自许可外，未明确授予的权利均予保留。
