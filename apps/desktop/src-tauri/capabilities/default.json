{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the baseline Tauri permissions required by the local writing window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
