# 墨舟：网络文学创作 AI 工作台完整开发计划

> 状态：M0–M27 已完成；M28–M36「AI 主写、作者主导」闭环进入实施
> 计划日期：2026-08-09，扩展日期：2026-08-11（AI 漫剧批次）、2026-08-12（幻想题材批次）
> 当前版本：0.2.0
> 适用范围：本地优先的中文网文创作工作台，正式支持历史重生、都市重生、东方玄幻与西方奇幻
> 实施约束：按里程碑增量实施；模型外发、文件解析和来源导入继续经过预览、确认与批准门

## 1. 计划结论

当前仓库不是空白原型，已经形成可运行的最小闭环：作品书架、可靠保存、章节状态、OpenAI 章纲与整章候选生成、正式事实与双时间线、人物/资源/伏笔账本、现实资料卡、连续性提示、项目归档恢复，以及多书分段拆解和模式卡应用都已存在。

下一阶段不应继续堆独立按钮，而应先把现有同步调用升级为可恢复任务平台，再围绕上下文编译、全局拆书资产、原创性门禁、整书总导演、多 Agent 审校和版本变更集形成一条真正的 AI 创作生产线。

建议的 MVP 完成定义是：

1. 作者输入一个创意后，AI 能生成可编辑的整书定位、卷级方向和未来 3 至 5 章计划。
2. AI 能在明确上下文、来源和成本下写出完整章节候选，作者不必手工填写所有字段。
3. 所有长任务可查看进度、取消、重试、断点恢复；应用退出或模型失败不会丢失已完成且已付费的结果。
4. 30 万字项目写到后期仍能召回最新事实、当前人物状态、开放伏笔、双时间线与已确认现实资料。
5. 多本长篇参考作品可在独立全局拆书库分段分析，抽象结果可跨项目复用；参考原文不进入正文生成上下文。
6. 参考驱动方案必须经过短语、专名、人物组合、关键节拍序列和组合风险门禁，高风险结果不能直接进入正文生成。
7. 连续性、节奏、人物、现实性、重生逻辑、文风和格式审校以带证据的问题单输出，并通过变更集接受、拒绝或局部应用。
8. macOS 和 Windows 安装包能在干净机器独立运行，升级、恢复、密钥、日志和用户数据都有明确安全边界。

单人全职开发预计 22 至 28 周达到封闭测试版；剧情沙盘作为 MVP 后的独立阶段，另需约 4 至 6 周。若有两名工程师，可将模型/后端主线与前端/桌面主线并行，预计压缩至 14 至 18 周。

## 2. 本次检查范围与证据

本次仅做只读检查和测试执行，没有修改现有代码或配置。检查范围包括：

- README.md
- docs/product/prd.md
- docs/product/development-plan.md
- docs/product 下 6 份专项规格
- tasks/plan.md 与 tasks/todo.md
- 根目录、Web、API、桌面端的依赖与构建配置
- React 页面、FastAPI 路由、SQLite schema、仓储、AI 网关、Tauri sidecar
- 前端、后端和 Rust 测试名称与覆盖场景

2026-08-09 实际测试结果：

| 测试层 | 结果 | 说明 |
|---|---:|---|
| Web Vitest | 26 / 26 通过 | 两个测试文件，主要覆盖 React 行为与 API 地址 |
| FastAPI pytest | 68 / 68 通过 | 覆盖数据、归档、AI、拆书、sidecar 与仓储 |
| Tauri Rust | 2 / 2 通过 | 覆盖动态端口和健康响应判断 |

未在本轮重复执行完整发布构建和安装包签名；README 记录的最近产物为 macOS arm64 DMG，当前目录也存在该构建产物。

## 3. 已交付基线

以下能力已经存在，后续计划只增强或重构，不重复立项：

### 3.1 作者资产可靠性

- 作品书架、新建、打开、退出后重新打开。
- 正文 700 毫秒自动保存；切章、进入拆书库、退出作品前强制落盘。
- revision 乐观并发控制；保存失败时阻止导航并保留本地草稿。
- 稳定的操作系统应用数据目录。
- SQLite schema 版本、升级前备份、完整性检查与未来版本拒绝。
- 项目完整归档、安全导入为副本、手动恢复点。

### 3.2 桌面运行

- Tauri 自动启动 FastAPI sidecar。
- 动态本地端口、健康门禁、退出回收。
- PyInstaller 单文件 sidecar 纳入桌面安装包。
- macOS arm64 DMG 已完成本机 ad-hoc 签名验证。

### 3.3 写作与 AI 最小闭环

- 章节目录、正文编辑、字数目标和待写/草稿/审校/定稿状态。
- 读者承诺、开篇钩子、状态变化、情绪回报、章尾悬念。
- OpenAI Responses API 生成候选章纲和完整章节。
- API Key 只保存在环境变量或当前进程内存，不进入项目和浏览器存储。
- 生成结果先作为候选，只有作者采用后才写入正文。
- 假模型、运行事件和 revision 冲突门禁。

### 3.4 故事连续性

- 正式事实与候选事实变更集。
- 原始时间线与小说时间线。
- 未来知识账本和分歧后的候选失效。
- 人物、资源、伏笔与现实资料卡。
- 三章节奏窗口、确定性连续性问题和断更恢复卡。

### 3.5 拆书实验室

- 项目内导入多本 UTF-8 TXT/Markdown。
- 默认每 50 万字进行章节感知切段。
- 每约 5 万字执行 Map，再跨书 Reduce 六个维度。
- 输出时代、核心欲望、冲突因果、资源体系、关键场景顺序和结局。
- 模式卡持久化，保留来源区段、共同规律、差异、人物关系重组和风险提示。
- 作者选择维度后应用为当前项目的抽象蓝图。
- 章节 AI 只读取已应用抽象字段，不读取参考原文。

## 4. 当前问题、缺口与风险

### 4.1 P0：继续扩展功能前必须解决

| 编号 | 问题 | 代码证据或现状 | 影响 | 处理阶段 |
|---|---|---|---|---|
| P0-01 | 当前目录不是 Git 仓库，也没有 CI | git status 无法识别仓库；无 .github 工作流 | 无可靠回滚、评审、版本追踪和跨平台发布证据 | M0 |
| P0-02 | 长篇拆书同步串行且不可恢复 | 最多 200 万字约 40 次 Map 加 1 次 Reduce；没有任务、块状态、取消和缓存 | 中途失败会丢失已完成且已付费结果，页面长时间等待 | M2 |
| P0-03 | 真实 AI 写章不可恢复 | generation resume 只处理 demo；真实运行异常只记 interrupted，前端也无任务列表 | 重启、断网或限流后不能安全续跑 | M2 |
| P0-04 | 后期事实不会进入 AI | story_facts 按最早创建排序，提示词只取前 50 条 | 长篇后期生成会忽略最新正式事实，直接破坏连续性 | M1、M4 |
| P0-05 | AI 面板在合法桌面宽度消失 | Tauri 最小宽度 1040，CSS 在 1050 以下隐藏整个 agent-panel | 用户会误以为产品没有 AI，核心能力不可达 | M1 |
| P0-06 | 没有阻断式原创性门禁 | 当前仅展示模型返回的 originality_risks 文本 | 参考蓝图可直接进入章节上下文，无法验证组合过近 | M6 |
| P0-07 | 计划、PRD 与实现状态漂移 | 旧开发清单仍把已交付能力标成未完成，PRD 状态仍为 M2 | 后续容易重复开发或遗漏真实断点 | M0 |

### 4.2 P1：MVP 体验与架构缺口

| 编号 | 缺口 | 当前行为 | 目标 |
|---|---|---|---|
| P1-01 | 拆书资产不能跨项目复用 | reference_works、模式卡都绑定 project_id | 建立全局拆书库，项目只保存应用关系和不可变蓝图快照 |
| P1-02 | 非 UTF-8 中文文本可能静默乱码 | 浏览器 File.text 按 UTF-8 解码，GB18030 无显式检测与预览确认 | 支持 UTF-8、UTF-8 BOM、GB18030，低置信度时让作者确认 |
| P1-03 | 现实资料只能手工建卡 | 没有 TXT/Markdown/PDF 文件导入与来源回链 | 本地解析、卡片化、页码或字符范围证据、确认状态 |
| P1-04 | 只有单章导演，没有整书总导演 | 新建项目后仍需作者逐字段维护 | 从一句创意生成整书定位、结局方向、卷纲和滚动章纲候选 |
| P1-05 | AI 流程仍是多次人工点选 | 设计章纲、采用、保存、写章分别执行 | 提供可配置的一键单章流水线，但仍停在候选与批准门 |
| P1-06 | 连续性检查以规则为主 | 没有七维多 Agent 问题单、证据合并与单维重跑 | 多维并行审校、问题去重、严重级别和建议变更集 |
| P1-07 | 缺少版本差异和局部应用 | 只有项目级恢复副本，生成候选只能整章采用 | 章节版本、diff、局部接受、回滚产生新版本 |
| P1-08 | 工作区一次加载整部正文 | Workspace 返回所有章节 content 与多数领域集合 | 项目增大后首屏、内存和每次刷新成本上升，需要摘要与分页 |
| P1-09 | 密钥重启即丢且只有一个提供商 | 进程内 OpenAI Key；没有 Keychain、base URL、能力探测 | 系统密钥库、多配置、OpenAI-compatible 契约、用量与限额 |
| P1-10 | API 契约前后端手工重复 | Pydantic 和 TypeScript 分别维护 | 生成或契约测试防止字段漂移 |
| P1-11 | 数据库升级不是显式迁移链 | 当前执行最新 schema 加少量补列后直接写 user_version | schema 复杂后难以验证逐版本升级和回滚 |
| P1-12 | 本地 API 缺少会话令牌 | 只依赖动态 loopback 端口 | 增加每次启动随机令牌，降低其他本机进程调用风险 |

### 4.3 发布与质量缺口

- 没有真实浏览器 E2E 测试和桌面端自动冒烟。
- 没有 30 万字创作项目、300 万字参考作品的性能基准。
- 没有断网、限流、进程退出、磁盘写满和数据库锁的故障注入。
- 没有 Windows 原生 sidecar 和安装包的持续验证。
- macOS 只有 ad-hoc 签名，没有 Developer ID、公证和升级矩阵。
- 没有覆盖率门、SBOM、NOTICE、依赖与密钥扫描的 CI 记录。
- 没有结构化脱敏日志、诊断包和用户可理解的恢复建议。
- 根目录开发命令使用 shell 后台符号，不适合 Windows，也不保证子进程回收。

### 4.4 产品需求缺口

- 缺少现有小说稿件导入后自动识别卷、章、正文的能力。
- 缺少卷级、场景级结构和可锁定字段。
- 缺少目标读者、核心卖点、长期承诺、结局方向和人物弧管理。
- 缺少搜索、快捷键、专注写作、日更目标、存稿与待发布看板。
- 缺少 Markdown 正文导出；DOCX、EPUB 仍未排期确认。
- 缺少资料研究员 Agent 和历史/都市重生的首批现实资料模板。
- 缺少调用前上下文预览、token/费用估算和调用后来源追踪。
- 缺少参考原文删除、缓存清理和外部发送范围的可审计记录。
- 缺少 MiroFish 思路中的角色/势力沙盘、变量注入、分支比较、角色采访和影响报告。

## 5. 已确定的产品与架构边界

以下决策已有对话或现有文档支持，不再重复询问：

1. 产品优先服务中文男频网文，首发历史重生与都市重生。
2. 首发面向男频作者，现实资料模型保持年代、地区和行业可扩展。
3. 默认本地优先、单机、单用户、用户自带模型密钥。
4. AI 是默认创作伙伴，不是只做提示的工具；必须能设计章纲和写完整章节。
5. 所有 AI 结果先作为候选，不能直接覆盖正文、正式事实或时间线。
6. 参考时允许保留、调整或重构时代、核心欲望、冲突因果、资源体系、关键场景顺序和结局；人物关系优先重组。
7. 一本长篇可按约 50 万字区段参考；一次分析可跨多本书和多个区段。
8. 全局拆书库与具体创作项目分离，拆出的模式卡可应用到任意项目。
9. 参考原文只接受作者自有、明确授权或公版作品，不支持付费站抓取、DRM 破解或来源不明语料。
10. MiroFish 只作为剧情沙盘的能力参考，不直接引入其 AGPL-3.0 源码、Zep Cloud 或社交媒体动作模型。
11. 原创性高风险方案不能靠二次点击直接绕过；必须调整或重生成并重新检测。
12. MVP 不做云端协作、支付、平台自动发布、移动端和无人值守整书批量生成。

## 6. 需要产品负责人决定的事项

以下问题会实质影响架构、许可、发布顺序或默认工作流。若接受推荐项，只需在审阅时回复“按推荐项执行”。

| 决策 | 选项 | 推荐项 | 最晚决策点 | 影响 |
|---|---|---|---|---|
| D1 产品分发与代码许可 | 闭源商业产品、开源项目、双许可证 | MVP 先按闭源私有测试管理；继续清洁实现，不直接复制 MiroFish 代码 | M0 结束前 | 第三方依赖政策、仓库可见性、NOTICE、商业化路径 |
| D2 AI 默认自动化程度 | 每一步手动确认；一键完成单章候选；允许连续多章候选 | 一键串联“上下文→章纲→审校→完整章节候选”，在写入正文前停止；多章仍逐章批准 | M7 开始前 | 任务编排、交互、成本确认、批准门 |
| D3 首轮是否强制离线模型 | 首轮必须 Ollama；只支持云端 BYOK；接口兼容但原生支持后置 | 首轮支持 OpenAI-compatible 和自定义 base URL；Ollama 原生安装与能力适配放 P1 | M3 开始前 | 模型网关、打包体积、能力降级、支持成本 |
| D4 发布平台顺序 | macOS 先内测再补 Windows；macOS/Windows 同步首测 | macOS 先做 5 至 10 人内部测试，Windows 在公开 MVP 前必须过发布门 | M0 结束前 | CI 机器、证书、周期和问题定位效率 |

项目归档在全局拆书库上线后的默认行为先采用可逆方案：普通项目归档包含已应用蓝图快照和来源元数据，不默认复制全局参考原文；另提供显式的“包含关联参考资产”完整归档。若审阅时有不同要求，可在 M5 前调整。

## 7. 目标架构

### 7.1 组件边界

~~~text
Tauri Desktop
  ├─ React Author Workbench
  │    ├─ Project / Editor / Director
  │    ├─ Global Reference Library
  │    ├─ Task Center
  │    ├─ Review & Change Sets
  │    └─ Settings / Diagnostics
  └─ Local FastAPI Sidecar
       ├─ Project & Canon Service
       ├─ Context Compiler
       ├─ Durable Job Runtime
       ├─ Model Gateway
       ├─ Reference Deconstruction
       ├─ Originality Guard
       ├─ Review Orchestrator
       └─ Import / Export / Recovery

SQLite
  ├─ Projects, chapters, versions, canon, sources
  ├─ Jobs, attempts, chunks, artifacts, events
  ├─ Global reference assets and derived pattern cards
  ├─ Project blueprint applications
  └─ Reviews, findings and change sets

System Services
  ├─ macOS Keychain / Windows Credential Manager
  ├─ Local file picker and safe parser
  └─ Optional OpenAI-compatible providers
~~~

### 7.2 核心数据边界

1. 正文、正式事实、现实资料、参考原文、抽象模式、沙盘假设必须使用不同表、模型和查询接口。
2. 正文生成只能读取 ContextPacket，不能自行查询整库。
3. ContextPacket 必须包含版本、来源、入选原因、token 预算和内容哈希。
4. 每次模型运行必须关联 job、attempt、provider、model、prompt/template version、context hash、用量和输出 artifact。
5. 全局参考原文只由拆书服务读取；正文服务只能读取已批准、已通过原创性门禁的项目蓝图快照。
6. 所有 AI 更新通过 Proposal 或 ChangeSet 进入作者批准门。
7. 章节回滚创建新版本，不删除或覆盖历史版本。
8. P0 检索使用结构化 SQL、规则评分和摘要，不依赖向量数据库；向量能力通过接口保留到 P1。

### 7.3 建议新增的领域对象

- Job、JobAttempt、JobChunk、JobArtifact、JobEvent
- ProviderProfile、ModelCapability、UsageRecord
- ContextPacket、ContextItem、ContextBudget
- BookBlueprint、VolumePlan、RollingChapterPlan、FieldLock
- ChapterVersion、TextChangeSet、ReviewRun、ReviewFinding
- GlobalReferenceWork、ReferenceFile、SegmentAnalysis、BookPatternCard、FusionPatternCard
- ProjectBlueprintApplication、OriginalityCheck、OriginalityFinding
- ImportSession、ParsedVolume、ParsedChapter、ExportProfile
- SandboxSnapshot、SimulationBranch、SimulationRound、SimulationReport（P1）

## 8. 阶段与里程碑总览

| 阶段 | 里程碑 | 预计单人周期 | 独立验收结果 |
|---|---|---:|---|
| A 基线与安全 | M0 工程治理与计划对齐 | 3 至 5 天 | 可回滚、可审计、跨平台可持续验证 |
| A 基线与安全 | M1 创作可信度热修 | 1 周 | 后期事实正确进入 AI，所有窗口宽度都能访问 AI |
| B AI 基础设施 | M2 可恢复任务运行时 | 2 至 3 周 | 写章和长篇拆书可进度、取消、重试、恢复 |
| B AI 基础设施 | M3 模型网关、密钥与用量 | 2 周 | 多 OpenAI-compatible 配置通过统一契约 |
| B AI 基础设施 | M4 长篇上下文编译器 | 2 至 3 周 | 30 万字项目仍能构建可解释、可预算上下文 |
| C 核心差异化 | M5 全局拆书库与资料导入 | 3 周 | 一次拆书、多项目复用，长篇分析分级缓存 |
| C 核心差异化 | M6 原创性门禁与蓝图工作流 | 2 至 3 周 | 高风险方案被阻断，六维可独立保留/调整/重构 |
| C 核心差异化 | M7 整书总导演与 AI 优先创作 | 3 周 | 从一句想法到前三章计划和单章完整候选 |
| C 核心差异化 | M8 多 Agent 审校、版本与变更集 | 3 周 | 七维审校带证据，可局部应用和安全回滚 |
| D 完整工作台 | M9 稿件导入、导出与连载体验 | 2 至 3 周 | 旧稿可接续、正文可导出、日更流程闭环 |
| D 发布硬化 | M10 规模、安全与双平台发布 | 3 周 | 规模、故障、安全、macOS/Windows 发布门通过 |
| D 产品验证 | M11 封闭测试与 MVP 发布 | 2 周 | 真实作者完成 10 章闭环，阻断问题清零 |
| P1 推演能力 | M12 剧情沙盘 | 4 至 6 周 | 角色/势力多分支推演只进入候选剧情 |
| E 原创性深化 | M13 场景语义与情节图检查 | 2 至 3 周 | 多书场景结构相似度可解释、可阻断、可重检 |
| E 推演智能化 | M14 AI 增强剧情沙盘 | 2 至 3 周 | AI 在规则约束内提出行动和后果，仍停在候选区 |
| E 资料生产线 | M15 资料研究员 Agent | 2 至 3 周 | 作者资料自动整理为带来源候选卡，确认后才生效 |
| E 文档互通 | M16 DOCX/EPUB 导入导出 | 2 周 | 常用长篇格式可安全预览、导入和无损导出 |
| E 作者工作流 | M17 作者效率与关系图谱 | 2 至 3 周 | 日历、批注、灵感与人物/伏笔图谱形成闭环 |
| F 漫剧基础 | M18 漫剧领域与可靠数据边界 | 1 至 2 周 | 小说来源可冻结，季/集/场与版本可恢复 |
| F 漫剧规划 | M19 AI 季纲与分集候选 | 1 至 2 周 | 可预览费用并生成一季候选分集 |
| F 漫剧生产 | M20 逐集剧本与双批准门 | 2 周 | 每集从已批准大纲生成完整候选剧本 |
| F 漫剧交付 | M21 审校、资产清单与制作包 | 1 周 | 已批准剧集可导出稳定制作包 |
| F 漫剧体验 | M22 AI 漫剧改编工作台 UI | 1 至 2 周 | 作者可在一个工作台完成核心闭环 |
| F 漫剧验收 | M23 完整度、浏览器与文档收尾 | 3 至 5 天 | 自动化与真实浏览器发布门通过 |

## 9. 里程碑详细计划

### M0：工程治理与计划对齐

#### 交付结果

- 建立私有 Git 仓库、主分支保护、提交规范和可恢复标签。
- 建立 macOS 与 Windows CI 验证矩阵。
- 把 PRD、开发计划、专项规格、任务清单与真实实现状态对齐。
- 冻结 0.1.0 数据、API、归档协议和测试基线。
- 记录关键架构决策，明确 MiroFish 仅参考能力、不复用代码。

#### 涉及模块

- 根目录工程配置、.gitignore、package scripts
- docs/product、docs/adr、tasks
- GitHub Actions 或选定的等价 CI
- Web、API、Tauri 构建命令

#### 前置依赖

- 产品负责人确认 D1 和 D4。
- 保存当前目录与用户数据库的独立备份。

#### 实施步骤

1. 初始化私有版本库，确认数据库、密钥、安装包、target、缓存和本地数据不会进入版本控制。
2. 创建基线标签并记录当前 26 前端、68 后端、2 Rust 测试结果。
3. 把根开发命令改为跨平台进程编排，确保 Ctrl+C 回收 Web 与 API。
4. 建立 lint、typecheck、unit、build、cargo、依赖扫描和 Windows 原生 sidecar CI。
5. 建立 ADR：本地优先、SQLite、Tauri sidecar、候选批准门、全局拆书库、任务运行时、MiroFish 清洁实现。
6. 更新文档状态矩阵，移除已完成能力的错误待办。

#### 测试方式

- 在全新目录从锁文件安装并执行完整 verify。
- 在 macOS 和 Windows CI 分别构建 sidecar 与桌面检查。
- 执行密钥和大文件扫描。
- 从基线标签创建临时分支并验证可回滚。

#### 验收标准

- 任一合并都必须通过自动发布门。
- 当前源码、依赖锁和文档都有可追踪版本。
- 用户数据库、参考原文、API Key 和构建产物不进入仓库。
- 旧计划不再把已经交付的 R1/R2 能力列为未完成。

#### 风险与回滚方式

- 风险：初始化仓库时误纳入用户数据。缓解：先只读清单和 ignore 检查，再提交。
- 风险：Windows CI 暂时不能构建 PyInstaller。回滚：保留 macOS CI 为必过，Windows job 标记阻断且不宣称支持 Windows。
- 回滚：任何脚本调整都可回到 0.1.0 基线标签；不触碰业务数据库。

### M1：创作可信度与核心可达性热修

#### 交付结果

- 修复最新正式事实无法进入 AI 的问题。
- 在 1040 像素及更窄窗口提供可访问的 AI 导演入口。
- 把工作区首屏拆成概要和按需正文接口的最小契约，为长篇分页铺路。
- 为数据库建立显式、可逐版本测试的迁移框架。
- 桌面 API 增加每次启动的随机会话令牌。

#### 涉及模块

- services/api/app/ai.py、repository.py、database.py、main.py
- packages/contracts
- apps/web/src/components/WorkspaceShell.tsx、DirectorPanel.tsx、styles.css、api.ts
- apps/desktop/src-tauri

#### 前置依赖

- M0 完成。

#### 实施步骤

1. 先加入超过 50 条正式事实的回归测试，证明最新事实当前会遗漏。
2. 短期按当前章节、最近更新、来源章节和实体相关性选择事实；M4 再替换为完整编译器。
3. 将窄窗口 AI 导演台改为抽屉、标签页或底部面板，不再 display none。
4. 增加项目概要与章节正文按需读取接口，保持旧 Workspace 契约一段迁移期。
5. 建立 migration 目录和 v1→v2→v3 顺序执行、升级前备份、失败不修改原库。
6. Tauri 启动 sidecar 时通过标准输入或受限环境传入随机令牌，Web 请求统一附带；令牌不写日志。

#### 测试方式

- 51 条以上事实的上下文选择回归测试，验证最新且相关的事实入选。
- 1040、900、760 三档真实浏览器与桌面窗口测试。
- API 令牌缺失、错误、正确三类测试。
- 从 schema v1 和 v2 夹具升级到新版本，并注入中途失败。

#### 验收标准

- 第 100 章生成上下文不会只包含最早 50 条事实。
- 在桌面允许的全部窗口宽度都能打开 AI 共创。
- 未持有本次启动令牌的本机 HTTP 请求不能调用业务 API。
- 旧数据库无损升级，失败时原库和备份均可恢复。

#### 风险与回滚方式

- 风险：事实排序热修引入无关信息。缓解：规则选择带来源和入选原因，M4 用黄金集校准。
- 风险：令牌握手导致桌面启动失败。回滚：保留带明显警告的兼容开关，仅用于开发 Web 模式。
- 回滚：新 API 与旧 Workspace 并存一版；迁移失败自动停止启动，不降级为空库。

### M2：可恢复 AI 任务运行时

#### 交付结果

- 统一 jobs、attempts、chunks、artifacts、events 模型。
- AI 写章和拆书 Map/Reduce 都转为后台持久任务。
- 提供任务中心、进度、取消、失败块重试、重启恢复和结果继续采用。
- 已成功处理块可复用，不因单块失败重复付费。

#### 涉及模块

- 新增 services/api/app/jobs
- database.py、repository.py、main.py、ai.py、generation.py、reference_lab.py
- packages/contracts
- 新增 Web TaskCenter、进度提示和恢复入口

#### 前置依赖

- M1 显式迁移框架。
- 任务状态机与幂等键 ADR 通过评审。

#### 实施步骤

1. 定义 queued、running、pause_requested、cancelled、succeeded、failed、interrupted 状态及合法转换。
2. 定义 job kind：chapter_brief、chapter_draft、reference_segment_map、reference_book_reduce、reference_fusion、review。
3. 每个外部调用形成 attempt；每个 5 万字块形成 chunk；输出写入不可变 artifact。
4. 使用数据库租约和心跳避免重复执行；sidecar 重启时把过期 running 任务标为 interrupted 并恢复。
5. 先迁移拆书任务，验证多块缓存和单块重试；再迁移真实写章。
6. 前端轮询或事件流展示进度、当前步骤、预计调用数、已用量、取消与重试。
7. 保留 fake/replay provider，让默认 CI 不访问网络。

#### 测试方式

- 状态机属性测试和非法转换测试。
- 在第 1、20、40 个 Map 块以及 Reduce 前后注入崩溃。
- 模拟断网、限流、超时、格式错误、取消和 sidecar 被杀。
- 验证重启后不产生重复 artifact、不重复应用正文、不重复收费块。
- 前端任务中心行为测试和真实桌面恢复测试。

#### 验收标准

- 200 万字拆书中第 39 块失败，只需重跑失败块和后续 Reduce。
- 应用退出后重新打开，任务进度和已完成结果仍存在。
- 作者可以取消未完成调用，已完成 artifact 可查看。
- 同一幂等键重复提交不会创建两次正文写入。

#### 风险与回滚方式

- 风险：后台 worker 与 SQLite 并发导致锁争用。缓解：单写者队列、短事务、WAL 和退避。
- 风险：任务 schema 过早泛化。缓解：先只覆盖写章和拆书两种真实垂直切片。
- 回滚：旧同步接口保留为开发兼容一版，但产品 UI 只走任务接口；新任务表可停止消费，不删除 artifact。

### M3：模型网关、密钥与用量治理

#### 交付结果

- 统一 OpenAI-compatible 模型网关与能力探测。
- 支持多个 provider profile、自定义 base URL、模型选择和任务级默认模型。
- 密钥进入 macOS Keychain 或 Windows Credential Manager。
- 支持流式增量、取消、超时、重试、结构化输出、用量和费用估算。
- 所有模型错误归一为作者可行动的提示。

#### 涉及模块

- services/api/app/providers、ai.py、jobs
- Tauri 系统密钥命令
- Web 设置页与任务成本预览
- contracts 和 provider 契约测试

#### 前置依赖

- M2 任务运行时。
- 产品负责人确认 D3。

#### 实施步骤

1. 从 AiGateway 中拆出 ProviderAdapter、Capability、ModelProfile 和 ErrorCategory。
2. 实现 OpenAI Responses 适配器与通用 OpenAI-compatible 适配器。
3. 通过 Tauri 命令写入和读取系统密钥；API 只获得任务执行所需密钥，不回传前端。
4. 建立结构化输出、流式、取消、超时、限流、用量的契约测试套件。
5. 为模型记录输入/输出 token、估算成本、耗时和重试次数。
6. 引入显式外发预览：数据类型、字符范围、预计 token、目标 provider。
7. 保留录制回放夹具用于 CI 和问题复现，录制内容默认脱敏。

#### 测试方式

- 两个 OpenAI-compatible 端点的同一契约套件。
- 无结构化输出、无流式、无取消能力时的降级测试。
- Keychain/Credential Manager 写入、读取、删除和日志泄漏扫描。
- 限流、401、403、429、5xx、超时和无效 JSON 测试。

#### 验收标准

- 切换提供商不改变上层 job 与 artifact 契约。
- 重启应用后无需重新输入密钥，项目归档和数据库中没有明文密钥。
- 作者在调用前看到预计外发范围和成本，在调用后看到实际用量。
- 默认测试全程不访问付费模型。

#### 风险与回滚方式

- 风险：不同兼容端点并不真正兼容。缓解：能力探测、适配器级降级和明确支持矩阵。
- 风险：系统密钥服务不可用。回滚：允许当前会话内存密钥，不允许明文持久化。
- 回滚：保留现有 OpenAI 适配器作为首个 profile，网关切换通过 feature flag 控制。

### M4：长篇上下文编译器

#### 交付结果

- 每次生成先形成可预览、可版本化、可复现的 ContextPacket。
- 依据当前章节、人物、最新事实、开放伏笔、时间线、未来知识和现实资料进行选择。
- 硬约束永不因预算不足静默丢弃。
- 30 万字项目在固定预算内仍能维持连续性。

#### 涉及模块

- 新增 services/api/app/context
- canon、repository、ai、jobs、models
- Web 上下文预览与来源面板
- 长篇黄金测试项目

#### 前置依赖

- M2 任务 artifact。
- M3 provider 能力和 token 估算。

#### 实施步骤

1. 定义 ContextPacket schema：硬约束、当前状态、近期摘要、召回片段、来源、原因、优先级和预算。
2. 按当前章节参与人物、地点、资源、开放伏笔、有效时间范围和最近更新计算候选。
3. 建立分层预算：系统与章纲、正式事实、当前人物、近期章节、远期摘要、现实资料、已批准蓝图。
4. 为历史原文生成可追溯摘要；保留回到章节和字符范围的引用。
5. 为双时间线和未来知识加入专用冲突检查，不把原始历史当成分歧后的必然结果。
6. 存储 ContextPacket hash 和 compiler version，使生成可重放。
7. 在 UI 展示“为何选入/为何未选入”，允许作者临时固定或排除条目。

#### 测试方式

- 历史重生 20 章、都市重生 30 章和 30 万字扩展夹具。
- 注入最新事实、旧事实覆盖、同名人物、已回收伏笔、候选失效知识。
- 预算边界和确定性快照测试。
- 人工标注相关项的召回率、无关项比例和硬约束遗漏率。

#### 验收标准

- 硬约束遗漏率为 0。
- 固定黄金集中关键事实、当前人物状态和开放伏笔召回率达到约定阈值，初始目标不低于 95%。
- 每个历史片段都有来源和入选原因。
- 同一数据、同一 compiler version 和预算生成相同 ContextPacket。

#### 风险与回滚方式

- 风险：纯规则召回不足。缓解：先以结构化字段和来源为主，P1 再引入可替换的本地向量索引。
- 风险：上下文过多提高成本。缓解：预算分层、摘要缓存和作者可见的成本预览。
- 回滚：保留 M1 的安全选择器作为降级策略，但禁止退回最早 50 条截断。

### M5：全局拆书库与现实资料导入

#### 交付结果

- 拆书库从项目页面升级为真正的全局作者资产库。
- 同一本参考作品只导入一次，结构卡可应用到任意项目。
- 长篇分析形成“处理块→区段卡→单书卡→跨书融合卡”三级可缓存资产。
- 支持 UTF-8、UTF-8 BOM、GB18030 TXT/Markdown；安全接入 PDF。
- 现实资料与参考小说分别导入、分别存储、分别确认和引用。

#### 涉及模块

- database migrations、repository、archive
- reference_lab、jobs、safe parsers
- GlobalReferenceLibraryPage、SourceLibrary
- 项目应用映射与独立资料库备份

#### 前置依赖

- M2 可恢复任务。
- M4 ContextPacket 边界。

#### 实施步骤

1. 把 reference_work 的所有权从 project_id 迁移为全局 library scope，建立项目应用关联。
2. 无损迁移现有项目内参考作品；同内容通过 hash 提示去重，不自动合并权利声明。
3. 为块、区段、单书、融合结果建立版本、来源谱系和缓存键。
4. 文件导入先检测编码和置信度，展示文本预览；不确定时禁止静默导入。
5. PDF 在受限解析路径中提取页码和文本，拒绝加密、超限、嵌入脚本和解析炸弹。
6. 现实资料导入生成候选资料卡，保留文件、页码/字符范围、日期、适用年代和可信度；作者确认后才进入 ContextPacket。
7. 全局资产删除时列出受影响项目；项目中的已应用蓝图快照仍可读，原文、索引和缓存按策略清理。
8. 项目归档默认保存蓝图快照；提供显式包含关联参考资产的完整归档。

#### 测试方式

- 一次导入、两个项目应用、删除与恢复测试。
- 300 万字作品生成 6 个连续区段，40 个以上块中断恢复。
- UTF-8、BOM、GB18030、混合编码、乱码和超限文件测试。
- PDF 恶意样例、页码来源、空文本和加密文件测试。
- 原文隔离、删除残留和项目归档往返测试。

#### 验收标准

- 导入一次的参考作品可在任意项目中选择模式卡，不复制原文。
- 300 万字分析中断后从最后成功块继续。
- 乱码不会静默进入数据库。
- 正文 ContextPacket、日志和普通 Workspace 响应都不包含参考原文。
- 删除原文后无原文和派生索引残留，已应用抽象蓝图仍可解释。

#### 风险与回滚方式

- 风险：project scope 到 global scope 的迁移破坏旧归档。缓解：旧归档解析器保留，导入时转换为全局资产和项目快照。
- 风险：PDF 解析面扩大攻击面。缓解：体积/页数/时间限制、低权限进程和恶意夹具。
- 回滚：旧项目表只读保留一个版本周期；迁移前自动备份，失败不删除旧记录。

### M6：原创性门禁与蓝图工作流

#### 交付结果

- 六个维度分别支持保留、调整、重构，并形成可编辑、可版本化蓝图。
- 人物关系重组成为显式步骤。
- 在蓝图进入整书规划器和章节生成前执行阻断式原创性检查。
- 风险报告包含证据、阈值理由和修改方向，但不声称法律结论。

#### 涉及模块

- 新增 originality_guard
- reference pattern cards、project blueprint applications
- director/context/jobs
- Web 蓝图编辑器和风险报告

#### 前置依赖

- M5 全局模式卡与来源谱系。
- 原创性合成黄金集与版权文案评审。

#### 实施步骤

1. 为每个维度记录 source、mode、author edits、generated variant、version 和 lock。
2. 建立人物、地点、组织、专名和关系组合的标准化抽取。
3. 实现长短语重合、专名碰撞、人物组合、关键节拍序列和多维组合规则。
4. 只对作者最终选择的组合评分，不机械要求六个维度全部改变。
5. high risk 阻断进入 Director 和 Writer；medium risk 需要显式查看；low risk 可继续。
6. 修改或重生成只重跑受影响维度和原创性检查。
7. 保存阈值版本和证据摘要，禁止把参考长段原文写进风险报告。

#### 测试方式

- 两部合成同题材作品、刻意近似变体、无关对照作品。
- 专名变体、人物关系同构、关键节拍换词、常见套路组合和边界阈值测试。
- 误报、漏报和阈值版本回归报告。
- 验证所有进入 Director 的参考蓝图都有 passed 检查。

#### 验收标准

- 高风险蓝图无法调用整书规划或正文生成接口。
- 修改一个维度后只重跑必要检查。
- 报告能回到来源区段和抽象证据，但不暴露长段原文。
- UI 明示“原创性风险提示，不是法律结论”。

#### 风险与回滚方式

- 风险：误报阻碍常见题材创作。缓解：多信号分级、合成基准和编辑复核，不依赖单一总分。
- 风险：漏报造成仿写风险。缓解：默认阻断高风险组合，P1 加入场景级语义与情节图检查。
- 回滚：阈值和规则版本化；可以回到上一已验证规则集，但不能关闭高风险门禁。

### M7：整书总导演与 AI 优先创作

#### 交付结果

- 作者输入一句创意或少量约束，AI 生成可编辑的项目启动方案。
- 建立目标读者、核心卖点、重生点、长期承诺、结局方向、人物弧、资源成长线、卷级方向和未来 3 至 5 章计划。
- 支持字段锁定、单字段重生成和冲突预览。
- 按 D2 提供单章 AI 流水线，最终停在候选稿。

#### 涉及模块

- 新增 planning/director
- project、chapters、context、jobs、review
- CreateProjectForm、Director workspace、rolling plan UI
- prompt/template registry 与运行记录

#### 前置依赖

- M3 模型网关。
- M4 ContextPacket。
- M6 参考蓝图门禁。
- 产品负责人确认 D2。

#### 实施步骤

1. 扩展项目 schema：受众、卖点、核心欲望、分歧点、长期承诺、结局方向。
2. 建立 BookBlueprint、VolumePlan、RollingChapterPlan 和场景节拍。
3. 从一句创意生成 2 至 3 个差异明确的启动候选，而不是直接写入正式项目。
4. 作者选择后生成主要人物功能、资源体系、第一卷方向和前三章计划。
5. 为字段增加 lock 和 dependency；重生成前显示将受影响的下游字段。
6. 单章流水线依次执行上下文、章纲、预审、正文候选；任何阶段可暂停和单独重跑。
7. AI 写章入口在编辑器和窄屏都保持突出，明确显示正在由 AI 完成什么。

#### 测试方式

- 历史重生、都市重生和现实资料锚点三个启动案例。
- 锁定结局、重生年份、人物关系后重生成测试。
- 依赖冲突、旧 revision、原创性阻断和成本超限测试。
- 真实作者从一句话到首章候选的可用性测试。

#### 验收标准

- 新作者不必手填全部章纲字段也能得到完整首章候选。
- 锁定字段在任何重生成中保持不变。
- 整书方案、卷纲和章纲都可编辑、可比较、可拒绝。
- 一键流水线绝不自动覆盖正文或正式事实。

#### 风险与回滚方式

- 风险：总导演输出宏大但不可写。缓解：每级计划必须有状态变化、资源变化、情绪回报和验证条件。
- 风险：自动链路成本不可控。缓解：调用前估算、阶段上限、按阶段选模和取消。
- 回滚：保留现有分步章纲与写章入口；流水线作为可关闭编排层。

### M8：多 Agent 审校、版本与变更集

#### 交付结果

- 连续性、连载节奏、人物、现实性、重生逻辑、文风和格式七维审校。
- 各维并行执行、独立重跑，输出统一问题 schema。
- 问题带严重度、证据、解释、建议、置信度和来源。
- 章节版本、文本 diff、部分接受和回滚产生新版本。
- 章节批准后由事实记录员生成候选事实变更集。

#### 涉及模块

- review orchestrator、review agents、jobs
- chapter_versions、review_findings、text_change_sets
- continuity、canon、context
- Web 审校台、diff 和部分应用 UI

#### 前置依赖

- M2 任务并行与 artifact。
- M4 可追溯上下文。
- M7 章节候选。

#### 实施步骤

1. 定义统一 ReviewFinding 和 ChangeSet 契约。
2. 把现有确定性连续性与三章节奏检查纳入同一审校台。
3. 增加五类 LLM/规则混合审校，并限制每类只回答自己的职责。
4. 按证据范围、问题类型和语义摘要合并重复问题。
5. 建立章节版本表；保存、生成、应用和回滚都创建版本事件。
6. 建立文本变更集，支持接受、拒绝、部分接受和作者编辑。
7. 章节批准后生成候选事实；只有作者确认才回灌 Canon。

#### 测试方式

- 中文黄金集覆盖事实冲突、称谓、知识泄漏、年代错置、双时间线、节奏、悬念、AI 套话和标点。
- 三章、十章连续审校和单维重跑。
- diff 边界、并发 revision、部分应用和回滚测试。
- 模型建议不能直接写正式事实的权限测试。

#### 验收标准

- 每个问题都有正文证据或结构化来源，不输出无证据总分。
- 一个维度失败不丢失其他维度结果。
- 作者可以只应用一条建议；过期建议不能覆盖新稿。
- 回滚产生新版本且完整保留历史。

#### 风险与回滚方式

- 风险：审校噪声过多。缓解：严重度、置信度、去重和作者接受率反馈。
- 风险：文风审校导致同质化。缓解：只指出可证据化问题，不定义唯一高分模板。
- 回滚：各审校维度可独立禁用；版本和变更集数据保持只增不改。

### M9：稿件导入、导出与连载工作台

#### 交付结果

- 导入已有 TXT/Markdown 稿件，自动识别卷章并提供人工校正预览。
- 支持卷、章、场景树的新增、重命名、移动和安全删除。
- Markdown 导出和项目完整备份形成清晰区别。
- 提供日更目标、存稿、待审、定稿、待发布看板。
- 完成搜索、快捷键、专注写作、无障碍和响应式核心路径。

#### 涉及模块

- import/export services
- volume/chapter/scene domain
- editor、project library、serial dashboard
- safe parser、archive、contracts

#### 前置依赖

- M8 章节版本和变更集。
- M5 编码检测与安全解析基础。

#### 实施步骤

1. 导入前检测编码、标题模式、卷章边界，展示树形预览和未识别段落。
2. 导入在单事务中创建项目、卷、章和初始版本；失败不留半成品。
3. 为目录操作定义稳定排序键、引用检查和撤销事件。
4. Markdown 导出只读生成，保留卷章顺序、标题和 UTF-8。
5. 建立今日目标、实际字数、存稿数、审校队列和发布准备状态。
6. 加入项目/章节/人物/伏笔搜索和常用键盘操作。
7. 用抽屉或可切换面板适配小桌面窗口，确保编辑器与 AI 都可达。

#### 测试方式

- 章节标题变体、无卷名、混合编码、超长章节和空章节导入。
- 导入后重新导出并比较结构和正文。
- 目录移动、删除引用保护、撤销和并发测试。
- 键盘、焦点、对比度、屏幕阅读器标签和多尺寸真实浏览器测试。

#### 验收标准

- 作者可在导入预览中修正结构，确认前数据库不写入。
- Markdown 导出顺序、标题、正文和编码准确。
- 30 万字项目的看板和搜索保持可用。
- 核心创作路径不用鼠标也能完成。

#### 风险与回滚方式

- 风险：标题正则错误拆章。缓解：预览、置信度、人工合并拆分，不直接提交。
- 风险：目录移动破坏来源引用。缓解：稳定 ID，不用章号作为外键。
- 回滚：导入整事务回滚；目录操作通过事件撤销；旧项目不强制场景化。

### M10：规模、安全与双平台发布硬化

#### 交付结果

- 30 万字项目、300 万字参考作品和 100 章规模达到性能目标。
- 完成日志、诊断、低磁盘、损坏库、升级与卸载数据保留策略。
- 完成 macOS 和 Windows 原生构建、签名、安装、升级和卸载验证。
- 建立 SBOM、NOTICE、依赖扫描、密钥扫描和发布清单。

#### 涉及模块

- repository 查询、分页、索引、前端状态管理
- desktop build scripts、Tauri、PyInstaller
- logging/diagnostics/security
- CI release pipeline

#### 前置依赖

- M0 至 M9 功能冻结。
- 发布证书和 Windows 构建环境。

#### 实施步骤

1. 建立冷启动、项目打开、切章、保存、检索、上下文和拆书基准。
2. 通过分页、按需正文、索引和组件拆分消除 monolithic Workspace 热点。
3. 建立结构化脱敏日志、job 诊断、数据库 quick_check 和一键诊断包。
4. 对文件导入、归档、提示注入、本地 API、密钥和日志做安全审查。
5. 建立 macOS Developer ID、公证和 Windows 代码签名流程。
6. 在干净虚拟机执行安装、首次启动、升级、卸载保留数据和恢复。
7. 生成 SBOM、NOTICE、校验和和可复现发布记录。

#### 测试方式

- 1、30、100 章和 30 万字项目性能矩阵。
- 300 万字参考作品、200 万字单次分析和任务恢复。
- 磁盘写满、SQLite busy、进程被杀、数据库损坏、断网和系统休眠。
- 当前两个受支持 macOS 主版本和 Windows 11 干净机冒烟。
- 依赖、许可证、密钥、CSP、本地 API 和恶意文件扫描。

#### 验收标准

- 项目打开、切章和保存达到发布前约定的交互目标；建议首屏 2 秒内、切章 300 毫秒内、保存确认 1 秒内。
- 所有故障都不静默丢稿，恢复路径可理解。
- 全新机器无需安装 Node、Python 或 Rust 即可运行。
- macOS 和 Windows 安装包都有签名、校验和和发布记录。
- 无未处置的高危安全或许可证问题。

#### 风险与回滚方式

- 风险：签名、公证或 Windows 工具链延误。缓解：M0 起持续跑双平台 CI，证书作为明确外部依赖。
- 风险：性能重构改变数据契约。缓解：新旧 API 并行、基准和归档往返门禁。
- 回滚：发布前保留上一稳定安装包；数据库升级前自动备份，不支持静默降级写入新库。

### M11：封闭测试与 MVP 发布

#### 交付结果

- 5 至 10 名真实网文作者完成从立项到至少 10 章的创作闭环。
- 建立质量、成本、可靠性、原创性和使用体验的可比较报告。
- 清零发布阻断问题，冻结 MVP 范围并发布。

#### 涉及模块

- 产品内反馈与本地可导出的诊断
- 评测项目、发布清单、文档和安装包
- 全部 P0 模块

#### 前置依赖

- M10 发布候选包。
- 试用作者、测试权利声明和隐私说明。

#### 实施步骤

1. 准备历史重生、都市重生和现实资料三个引导模板。
2. 为每位作者建立任务：新建或导入、规划、AI 写章、审校、事实回灌、拆书应用、导出恢复。
3. 收集完成率、人工修改比例、建议接受率、每章耗时、模型成本、失败恢复率和严重连续性遗漏。
4. 每周只修复阻断和高频问题，不在测试期扩大范围。
5. 运行最终数据迁移、安装升级、安全、许可证和原创性门禁评审。
6. 发布带版本说明、已知限制、恢复说明和校验和的 MVP。

#### 测试方式

- 真实作者可用性测试和盲评。
- 连续 10 章端到端回归。
- 发布候选包全矩阵验证。
- 从上一版本真实数据库升级并恢复演练。

#### 验收标准

- 100% 测试作者能够找回项目和最近正文。
- 关键任务崩溃恢复不产生重复写入或已付费结果丢失。
- 高风险参考方案无法绕过门禁进入正文生成。
- 严重连续性问题、丢稿、数据损坏、密钥泄漏和安装阻断为 0。
- 试用作者能明确感知 AI 在写，而不是被迫手填全部内容。

#### 风险与回滚方式

- 风险：模型质量与产品流程问题混在一起。缓解：固定模型/模板基线并记录版本和上下文。
- 风险：测试作者使用未授权参考作品。缓解：测试协议、权利声明和合成参考集。
- 回滚：不满足发布门时保持私测，不公开发布；上一稳定构建与数据库备份可恢复。

### M12：P1 剧情沙盘（MiroFish 能力的清洁实现）

#### 交付结果

- 从正式事实、双时间线和现实资料创建不可变场景快照。
- 支持 5 至 20 个角色或势力进行 3 至 10 轮受约束推演。
- 支持变量注入、多个分支比较、角色采访和影响报告。
- 沙盘结果只形成候选剧情分支，不能直接写正文、事实或时间线。

#### 涉及模块

- 新增 narrative_sandbox
- canon/context/jobs/provider gateway
- 分支比较、角色采访和报告 UI
- 候选章纲与变更集接口

#### 前置依赖

- MVP 章节闭环、任务运行时和批准门稳定。
- MiroFish 许可边界 ADR 保持有效。

#### 实施步骤

1. 定义角色可用知识、位置、资源、关系、目标和行动约束。
2. 从已批准项目状态创建不可变 SandboxSnapshot。
3. 每轮只允许符合能力和知识边界的行动，保存假设、证据和状态变化。
4. 同一快照运行多个变量分支，保证互不污染。
5. 生成角色采访和综合报告，标明推演而非事实。
6. 作者选择后只能进入候选卷纲、章纲或事实变更集。
7. 不引入 MiroFish 源码、AGPL 依赖、Zep Cloud 和 Twitter/Reddit 动作模型。

#### 测试方式

- 非法行动、知识越权、资源不足、位置冲突和快照不可变测试。
- 5/20 个角色、3/10 轮、取消、重启恢复和预算上限。
- 分支隔离、同种子可重放和候选批准门测试。
- 历史势力与都市商战固定场景人工评审。

#### 验收标准

- 沙盘无法修改正式项目状态。
- 每条结论包含假设、证据、置信度和影响链。
- 多分支结果可比较且互不污染。
- 结果能给作者新剧情可能性，但不会伪装成历史或现实真相。

#### 风险与回滚方式

- 风险：推演结果显得权威。缓解：全程标注假设、置信度和反例，不输出唯一预测。
- 风险：Agent 数量导致成本爆炸。缓解：轮数、角色数、token 和总费用硬限制。
- 回滚：沙盘是独立 P1 模块，可整体关闭；候选数据不影响正式创作库。

### M13：场景级语义与情节图原创性检查

#### 交付结果

- 把参考模式卡和当前作品蓝图解析为场景节点、因果边、角色职能和资源流向。
- 以版本化、确定性算法比较多本参考作品与当前方案，输出节点语义、连续子图、因果链和角色职能同构风险。
- 每条发现保留来源模式卡/区段、摘要、哈希、相似度、解释和可执行重构建议，不保存命中原句。
- high 风险阻断章纲、正文和沙盘 AI 外发；medium 必须查看报告并确认；修改场景后可局部重检。

#### 涉及模块

- `scene_originality` 领域服务与 schema v18
- 全局拆书模式卡、项目蓝图、原创性门禁、ContextPacket
- FastAPI/TypeScript 契约、拆书实验室原创性报告 UI
- 项目归档 v7 与诊断脱敏规则

#### 前置依赖

- M5 全局参考资产、M6 原创性门禁和 M7 整书蓝图已稳定。
- 只读取作者已授权或公版参考资产的抽象模式，不读取未选择原文。

#### 实施步骤

1. 定义 `SceneNode`、`PlotEdge`、`SceneGraph`、`SceneSimilarityFinding` 和规则版本。
2. 从六维模式卡与应用蓝图确定性提取场景图，使用中文规范化、字符 n-gram、关键词集合、顺序 LCS 和带类型边匹配评分。
3. 对多本来源分别评分，再计算跨来源组合风险；保存来源范围与证据哈希。
4. 建立 low/medium/high 阈值、报告查看确认和场景变更后的局部重检。
5. 把最新场景图检查接入全部章纲、正文和 AI 沙盘调用前门禁。
6. 在拆书实验室展示节点/边差异、风险来源和“保留逻辑但重构表达”的建议。

#### 测试方式

- 同义改写、换名不换因果、节拍乱序、常见套路、连续子图和多书拼接黄金集。
- 规则确定性、同版本重放、来源删除、蓝图 revision 过期和局部重检测试。
- high 阻断所有生成入口、medium 查看确认、报告无原句和归档往返测试。
- 1040/760 像素真实浏览器检查报告可达性、图谱布局和控制台。

#### 验收标准

- 黄金集中的结构近似 high 样本全部阻断，常见套路不被误判为 high。
- 同输入、同规则版本的分数与发现哈希完全一致。
- 报告可解释具体节点/边为何相似，但不泄漏参考原句，也不声称法律结论。
- 未通过场景检查的方案不能进入任何正文生成或 AI 沙盘外发。

#### 风险与回滚方式

- 风险：中文语义仅靠规则召回不足。缓解：首版以结构化图和多信号为主，保留可替换的嵌入接口，不把单一分数当结论。
- 风险：误报阻碍通用套路。缓解：常见模式降权、多信号才 high、medium 允许显式确认。
- 回滚：关闭新门禁时仍保留 M6 旧门禁；v18 表为附加数据，不改写参考原文或正文。

### M14：AI 增强剧情沙盘

#### 交付结果

- 在现有确定性沙盘上增加可选 AI 模式，由模型为每方提出受约束行动、动机和候选后果。
- 调用前展示快照来源、角色数、轮数、发送字符、Token、费用和目标线路；作者确认后才创建可恢复任务。
- 服务端重新校验知识、位置、能力、资源与预算，非法模型行动拒绝并使用确定性安全回退。
- AI 结果保留 Prompt/规则版本、上下文哈希、Attempt/Artifact 与成本；只形成沙盘轮次和隔离候选。

#### 涉及模块

- schema v19、`JobKind.SANDBOX_AI_ROUND`、模型任务类型与网关结构化输出
- `narrative_sandbox`、Job Runtime、provider profile、用量统计
- 沙盘外发预览、AI/规则模式切换、进度/取消/恢复 UI
- 场景原创性门禁和候选批准门

#### 前置依赖

- M2/M3 可恢复任务与费用治理、M12 沙盘约束、M13 场景门禁。
- 至少一条支持结构化输出或 JSON 降级的 OpenAI-compatible 线路。

#### 实施步骤

1. 定义 `SandboxAiActionDraft`、`SandboxAiRoundDraft` 与严格结构化 Prompt。
2. 冻结最小外发上下文，只含快照摘要、角色可知信息、当前状态、分支变量和允许行动。
3. 建立预览、费用上限、显式确认和 `SANDBOX_AI_ROUND` 单轮任务。
4. 对模型动作逐项重新校验；记录拒绝原因并以确定性行动补位，绝不放宽规则。
5. 将合法动作原子写入新轮次，支持取消、失败重试、重启恢复和同一 Artifact 重放。
6. 报告区分“模型建议”“规则裁决”“安全回退”，候选继续使用 M12 隔离门。

#### 测试方式

- 假模型覆盖合法输出、知识越权、资源不足、未知角色、格式错误、超时和重复提交。
- 费用上限、未确认外发、M13 high 风险、取消、重启和 Artifact 重放测试。
- AI/规则分支隔离、正式正文/Canon/时间线零变化测试。
- 本地兼容协议真实浏览器验收，不调用付费模型。

#### 验收标准

- 未确认外发或费用超限时模型调用次数为 0。
- 任何非法 AI 行动都不能进入状态；安全回退后轮次仍可完成并可解释。
- 失败重试不重复计入已完成轮次，不丢失已付费 Artifact。
- AI 沙盘批准仍不能直接写正文、正式事实或时间线。

#### 风险与回滚方式

- 风险：模型成本随角色数放大。缓解：每轮一次结构化调用、角色/轮数/Token/费用硬上限。
- 风险：模型输出看似权威。缓解：报告区分提议与裁决，持续展示假设、置信度和反例。
- 回滚：UI 默认保持规则模式；移除 AI handler 不影响既有规则沙盘与历史轮次。

### M15：资料研究员 Agent

#### 交付结果

- 作者可从已导入 TXT/Markdown/PDF/DOCX/EPUB 资料或粘贴文本创建研究任务，指定年代、地区、主题和资料类型。
- Agent 把材料整理为历史事件、地方制度/地理、行业规则、价格/技术条件和争议点候选卡。
- 每张候选卡必须包含来源文档、页码/字符范围、适用年代、置信度、冲突提示和原文哈希。
- 候选必须逐条批准才进入现实资料卡；研究材料和未确认结论不进入正文上下文。

#### 涉及模块

- schema v20、`JobKind.RESEARCH_EXTRACTION`、research service/repository
- 全局资料码头、安全文件解析、模型网关和 Job Runtime
- 研究会话、来源选择、外发预览、候选卡审核 UI
- SourceCard、ContextPacket 与项目归档 v8

#### 前置依赖

- M3 模型线路、M5 安全资料导入、M2 可恢复任务。
- 作者明确拥有材料使用权；首版不抓取登录页、付费站或未知来源页面。

#### 实施步骤

1. 定义研究会话、来源快照、研究发现、冲突组和候选卡模型。
2. 支持选择现有全局资料或粘贴最多 20 万字的本地材料，先预览来源范围与哈希。
3. 先用本地规则提取日期、地点、数量和主题片段，再由可选模型归纳为结构化候选。
4. 检查所有引用范围确实存在于来源快照；无可验证证据的模型结论丢弃。
5. 保存研究任务进度、失败块和用量；相同来源哈希与研究问题复用 Artifact。
6. 作者逐条批准后复用 SourceCard 确认流程，冲突资料并列保留而不是自动裁决。

#### 测试方式

- 历史年表、地方志片段、行业价格表、多来源冲突和无日期材料黄金夹具。
- Prompt 注入文本、伪造引用、越界字符范围、空文件、超限和模型格式错误测试。
- 未确认外发、费用限制、取消恢复、缓存复用和候选批准门测试。
- 报告/诊断/归档隐私与真实浏览器审核流程。

#### 验收标准

- 所有可批准候选都有可回查来源范围；伪造或越界引用接受率为 0。
- 未批准研究发现不进入 ContextPacket；批准后标记来源、年代和置信度。
- 同一材料的冲突结论不会被静默合并成唯一事实。
- 产品不自动上传、抓取或发送材料，所有外发均由作者确认。

#### 风险与回滚方式

- 风险：研究结论错误或来源低质。缓解：候选制、证据回链、冲突并列和“不是事实核验服务”提示。
- 风险：资料含 Prompt 注入。缓解：来源作为不可信数据封装，结构化输出和服务端引用验证。
- 回滚：隐藏研究员入口并保留手工 SourceCard；研究表可独立迁移，不改变已确认资料卡。

### M16：DOCX/EPUB 安全导入导出

#### 交付结果

- 旧稿接续支持 DOCX 与 EPUB 的本地安全预览、卷章识别、作者校正和事务导入。
- 正文支持 Markdown、DOCX 与 EPUB 三种独立导出；保持卷章顺序、标题、段落、中文和正文内容。
- DOCX 不执行宏/OLE/外部模板，EPUB 不执行脚本/远程资源；压缩包防路径穿越和解压炸弹。

#### 涉及模块

- `safe_import`、`manuscript/importer.py`、`manuscript/service.py`
- ZIP/XML/XHTML 安全解析与 DOCX/EPUB 生成器
- FastAPI 文件预览/导出、前端文件选择和下载
- 导入导出回归夹具与发布依赖清单

#### 前置依赖

- M9 稿件预览/事务导入、稳定卷章目录和 Markdown 导出。
- 解析只使用锁定依赖或 Python 标准库；新增依赖必须通过许可证和漏洞门。

#### 实施步骤

1. 建立 ZIP 条目数、压缩/解压大小、压缩比、规范路径、加密、软链接和活动内容检查。
2. DOCX 只读取 `word/document.xml` 与安全关系，映射标题样式和段落；拒绝宏、OLE、外链模板和异常包。
3. EPUB 读取 container/OPF/spine，按顺序提取本地 XHTML 文本；拒绝脚本、远程 URL、加密与异常包。
4. 复用现有卷章预览和人工修正流程，确认前零数据库写入。
5. 用最小合法 OOXML 与 EPUB 3 包生成导出文件，记录 SHA-256、卷章数和 MIME。
6. 前端根据格式使用二进制下载，并明确正文导出与项目归档的区别。

#### 测试方式

- 中文标题样式、分页、无标题段落、嵌套目录、混合 XHTML、特殊字符和空章节。
- Zip Slip、绝对路径、宏、脚本、远程资源、加密包、超限条目和压缩炸弹测试。
- TXT/MD/DOCX/EPUB 导入→导出→再导入结构与正文比较。
- macOS/Windows CI 和真实浏览器文件选择/下载验收。

#### 验收标准

- 安全样本导入预览准确，确认前不写库；恶意样本全部被明确拒绝。
- DOCX/EPUB 导出能被标准阅读器打开，卷章顺序和纯正文逐字符一致。
- 文件解析不访问网络、不执行活动内容、不写入工作区外路径。
- 原有 TXT/Markdown/PDF 资料导入与 Markdown 导出全部保持兼容。

#### 风险与回滚方式

- 风险：复杂排版无法无损。缓解：产品只承诺标题、段落和纯正文，不承诺图片、脚注或样式往返。
- 风险：ZIP/XML 攻击。缓解：解压前目录审计、硬限制、禁 DTD/外部实体和隔离内存解析。
- 回滚：移除新格式入口即可；旧导入与 Markdown 导出不变，不新增不可逆正文格式。

### M17：作者效率、批注、灵感与关系图谱

#### 交付结果

- 连载台增加周/月写作日历，展示每日净新增、目标和达成状态。
- 章节支持锚定 revision 与字符范围的批注；正文变化导致范围不匹配时标记待定位，不静默漂移。
- 全局灵感箱支持想法、标签、状态和应用为章纲/资料/人物候选，仍经过对应批准门。
- 人物关系图与伏笔图谱从正式账本生成，可筛选、定位来源章节和查看状态变化。

#### 涉及模块

- schema v21、author productivity service/repository
- ChapterVersion、SerialService、StoryEntity、StoryThread
- 连载台、编辑器批注层、灵感箱与 SVG/HTML 图谱 UI
- 项目归档 v9、搜索、快捷键与可访问性

#### 前置依赖

- M8 章节版本、M9 日更统计、人物/资源/伏笔正式账本稳定。
- 图谱只读取正式对象；AI 候选和沙盘假设不能伪装为正式节点。

#### 实施步骤

1. 定义 `ChapterAnnotation`、`AuthorIdea`、`StoryRelationship` 和 revision/状态机。
2. 将现有版本增量聚合为 42 天日历，并保留时区与日目标来源。
3. 创建/编辑/解决批注，保存正文 revision、内容哈希、选区和上下文；正文变化时重新定位或标记过期。
4. 建立灵感箱 CRUD、标签/状态筛选和“转为候选”动作，不直接写正式域。
5. 从显式人物关系与伏笔来源/回收章生成稳定节点和边，提供列表回退、筛选和章节定位。
6. 加入 `Cmd/Ctrl+Shift+I` 灵感箱、编辑器批注入口和窄屏单列模式。

#### 测试方式

- 跨时区、跨月、回滚版本、删除文本、重复文字和过期选区测试。
- 灵感创建/筛选/状态/候选转换及正式数据零直写测试。
- 循环人物关系、孤立节点、开放/回收/放弃伏笔和删除章节引用测试。
- 键盘、焦点、屏幕阅读器标签、1040/760 像素与 100 章性能测试。

#### 验收标准

- 42 天日历与章节版本增量一致，目标达成计算可重放。
- 批注不会因正文修改指向错误文本；无法安全重定位时明确显示过期。
- 灵感应用只创建候选，不直接改变正文、Canon 或时间线。
- 图谱节点/边都有正式来源，可从图谱定位人物、伏笔或章节；窄屏无横向溢出。

#### 风险与回滚方式

- 风险：图谱在长篇中过密。缓解：按角色/状态/章节窗口筛选、边聚合和列表回退。
- 风险：批注锚点漂移。缓解：revision、内容哈希、引用文本与上下文联合校验，失败即过期。
- 回滚：效率面板可整体隐藏；正文、版本、人物和伏笔原表不因新模块删除或改写。

### M18：漫剧领域与可靠数据边界

> 状态：已完成（schema v22、归档 v10、来源快照与独立漫剧域）

#### 交付结果

- schema v22 建立独立 `ComicProject`、`ComicEpisode`、`ComicVersion` 和 `ComicScene`。
- 作者可从同一小说的连续有效章节创建漫剧项目，冻结来源 revision 与 SHA-256。
- 提供创建、列表、详情和删除前影响查询的领域/API 契约。
- 项目归档 v10 保存并重映射漫剧项目、剧集、版本和场次。

#### 涉及模块

- `migrations/v22.py`、`database.py`、`archive.py`
- `comic_drama.py`、`models.py`、FastAPI 路由
- TypeScript contracts/API
- 数据库、归档和领域专项测试

#### 前置依赖

- M9 稳定卷章场景目录、M8 只增版本和现有 v21 迁移链。
- PRD 已冻结“文字制作包，不生成最终视频”。

#### 实施步骤

1. 定义严格枚举、结构化季纲/大纲/剧本契约和长度上限。
2. 新增四张领域表及 project/episode/source 索引，外键全部级联或显式置空。
3. 创建时验证章节同项目、未删除、无重复且按目录连续；计算规范来源快照。
4. 建立列表和详情聚合查询，候选与正式场次分层返回。
5. 归档升级到 v10，旧归档新增空表，恢复时重映射 JSON 内来源 ID。

#### 测试方式

- v21→v22、空库、重复启动、故障回滚和未来版本拒绝。
- 合法连续选择、跨项目、重复、不连续、删除章节和越界输入。
- v9→v10 空升级、v10 往返、恢复副本 ID/JSON 重映射和校验和。

#### 验收标准

- 漫剧数据与小说目录物理分离，创建或恢复失败不留半成品。
- 来源快照能稳定重放，同输入产生同 SHA-256。
- 旧用户库无损升级；旧归档可导入，包含漫剧的归档可完整恢复。

#### 风险与回滚方式

- 风险：JSON 内嵌来源 ID 漏重映射。缓解：归档往返逐字段断言和外部 ID 拒绝。
- 风险：迁移损伤用户库。缓解：继续使用 staged copy、quick_check 和升级前备份。
- 回滚：页面和 API 可隐藏；新表全为附加域，不改小说原表。数据库降级只允许恢复升级前备份。

### M19：AI 季纲与分集候选

> 状态：已完成（费用预览、可恢复候选任务与季方案采用）

#### 交付结果

- 新增 `comic_season_plan` 可恢复任务和漫剧默认模型线路。
- 调用前展示外发数据、字符/Token/费用、来源摘要和模型信息。
- 模型返回季纲与目标集数分集大纲，保存为候选版本和 Artifact。
- 作者采用季方案后创建候选剧集，但不批准单集大纲。

#### 涉及模块

- JobKind、AiTaskType、provider gateway 和 prompt
- `ComicDramaService` source compiler、preview/submit/handler
- Job/Chunk/Attempt/Artifact、任务中心标签
- FastAPI/TypeScript API 和任务专项测试

#### 前置依赖

- M18 来源快照与版本表。
- 现有模型线路、费用估算、JobRuntime 和会话令牌边界。

#### 实施步骤

1. 以每章结构摘要加受预算正文构建不可信来源上下文，保存冻结 Artifact。
2. 扩展 AI gateway 结构化输出，Prompt 明确 AI 漫剧、画面化和来源白名单。
3. 建立 preview、submit、result 和 adopt API；提交要求来源 SHA、外发确认和费用上限。
4. 用 provider/model/prompt/source/config 构建幂等键；重试复用已有 Artifact。
5. 校验目标集数、集号唯一连续、来源属于冻结集合、字段长度和结构完整性。

#### 测试方式

- 预览零模型调用、未确认外发、来源变化、费用超限和未配置线路。
- 幂等提交、运行成功、Provider 失败、重试复用 Artifact 和取消。
- 模型伪造章节 ID、集数不符、重复集号和超长字段拒绝。
- 采用季方案只创建候选剧集，小说正文/事实/时间线零写入。

#### 验收标准

- 作者确认前不外发；运行前后来源范围一致。
- 已付费结果在任务重试后不重复调用模型。
- 采用季方案后得到目标数量剧集，所有大纲状态仍为 candidate。

#### 风险与回滚方式

- 风险：长卷超过上下文。缓解：确定性预算编译、逐章公平配额和可见截断统计。
- 风险：模型输出结构不稳定。缓解：严格 structured output、来源白名单和安全错误。
- 回滚：禁用新任务 handler 与入口；候选 Artifact 保留，不影响既有任务。

### M20：逐集剧本与双批准门

> 状态：已完成（大纲批准、剧本批准、正式场次物化与历史版本保留）

#### 交付结果

- 作者逐集批准或拒绝分集大纲，旧 revision 操作被阻断。
- `comic_episode_script` 只为已批准大纲生成完整结构化剧本候选。
- 作者逐集批准剧本后物化正式场次；新候选不覆盖旧批准版本。
- 前集批准结果进入后集上下文，但改编事件不回写小说正式域。

#### 涉及模块

- 漫剧版本状态机与 scene materializer
- AI gateway、剧集上下文、JobRuntime
- 审核/拒绝/API contracts
- 状态机、来源和恢复专项测试

#### 前置依赖

- M19 已采用季方案和候选剧集。
- M18 版本表与来源快照。

#### 实施步骤

1. 实现单集大纲 approve/reject，批准时增加 episode revision。
2. 剧本 preview/submit 读取已批准季纲、大纲、相邻集、正式人物事实和前集批准摘要。
3. 生成场号、内外景、地点、时间、动作、对白、旁白、画面重点、钩子和资产需求。
4. 校验剧集号、来源白名单、场号连续、对白和动作结构。
5. 剧本 approve 时在一个事务中拒绝旧候选、记录批准版本并重建该集正式场次。

#### 测试方式

- 未批准大纲不能写剧本、旧 revision、重复批准和拒绝后重生成。
- 剧本任务预览/确认/幂等/恢复与恶意来源 ID。
- 批准前 `comic_scenes` 为零；批准后场次数一致；小说表逐表零变化。
- 批准第二个剧本版本保留第一个历史版本且正式场次指向新版本。

#### 验收标准

- 分集大纲和剧本两道批准门均不可绕过。
- 只有批准剧本进入正式场次和后续制作包。
- 任何失败或并发冲突都不产生半套场次或覆盖小说数据。

#### 风险与回滚方式

- 风险：剧本过度依赖旁白。缓解：字段级动作/对白最低要求和审校提示。
- 风险：重生成破坏连戏。缓解：前集批准摘要和来源哈希进入上下文，旧版本保留。
- 回滚：停用剧本 handler；已批准场次继续只读可导出。

### M21：审校、资产清单与制作包

> 状态：已完成（确定性审校、资产清单与 Markdown/JSON/DOCX 导出）

#### 交付结果

- 每集提供零费用确定性节奏、连续性、画面化和制作复杂度检查。
- 从批准场次生成角色、地点、服装、道具和特效资产清单。
- 导出 Markdown、JSON 和 DOCX 制作包，包含来源、版本、SHA-256 和 AIGC 提示。

#### 涉及模块

- `comic_drama.py` audit/export service
- FastAPI 二进制/JSON 响应和 Web 下载 API
- DOCX 最小安全 OOXML 生成
- 导出与确定性专项测试

#### 前置依赖

- M20 至少一个已批准剧本。
- M16 安全文档生成边界和文件名清理策略。

#### 实施步骤

1. 定义 error/warning/info 问题 schema 和稳定规则版本。
2. 检查钩子、冲突、动作/对白、时长、来源、资产密度和相邻集连戏。
3. 以规范名称和类型聚合资产，记录首次出现和出现剧集。
4. 构建稳定 JSON 协议、可读 Markdown 与 DOCX；只读取批准版本。
5. 在包内加入 `ai_generated`、生成链路和发布前标识/审核清单。

#### 测试方式

- 缺钩子、空动作、无对白、时长偏离、无来源和资产爆炸固定样本。
- 相同输入审校/资产/导出 SHA 稳定。
- candidate 不得进入导出；集数与场次排序准确。
- JSON schema、Markdown 内容和 DOCX 可打开/纯文字比较。

#### 验收标准

- 每条审校问题可定位到剧集或场次，不输出无法解释的总分。
- 资产清单可追踪首次出现和全部使用集。
- 三种格式只含批准剧集，校验和、版本和 AIGC 说明完整。

#### 风险与回滚方式

- 风险：规则把创作选择误判成错误。缓解：除结构缺失外默认 warning，不自动改稿。
- 风险：制作包被误认为可直接发布。缓解：明确“文字制作包”和发布前检查清单。
- 回滚：关闭导出入口；底层批准剧本不受影响。

### M22：AI 漫剧改编工作台 UI

> 状态：已完成（胶片轨工作台、候选确认、逐集审核与响应式界面）

#### 交付结果

- 工作区新增清晰可达的“漫剧改编”入口和独立页面。
- 作者可完成创建、预览费用、生成季方案、采用、逐集批纲、生成/批准剧本、查看审校和导出。
- 剧集胶片轨显示双道批准状态；中央画布始终只处理当前阶段。

#### 涉及模块

- `ComicDramaWorkbenchPage.tsx` 与测试
- `App.tsx`、`WorkspaceShell.tsx`、`api.ts`、contracts
- `styles.css`、任务中心和模型设置标签

#### 前置依赖

- M18–M21 API 和状态机稳定。
- 当前墨舟视觉系统和 1040/760 响应式边界。

#### 实施步骤

1. 创建来源选择与项目参数表单，默认 9:16、平衡改编。
2. 实现季纲候选、外发/费用确认和任务进度恢复。
3. 实现剧集轨、大纲批准、剧本候选与逐场次阅读。
4. 展示审校和资产清单，提供三种导出。
5. 加入空态、错误恢复、Escape/焦点、ARIA 状态和窄屏布局。

#### 测试方式

- React 行为覆盖创建、费用确认、季方案采用、大纲批准、剧本批准和导出调用。
- 加载、空态、失败、任务恢复和旧 revision 错误。
- 1280/1040/760 真实浏览器截图、尺寸、键盘和控制台检查。

#### 验收标准

- 作者不用进入任务中心或手写 JSON 即可完成一集闭环。
- 候选与已批准状态用文字和视觉双重编码，危险操作没有歧义。
- 760px 页面无整体横向溢出，核心按钮和当前集始终可达。

#### 风险与回滚方式

- 风险：长剧集列表和场次导致页面过重。缓解：按当前集加载、content-visibility 和派生数据 memo。
- 风险：新入口挤压写作头部。缓解：短标签和窄屏折叠，不隐藏核心写作入口。
- 回滚：移除页面入口；后端数据和导出仍可保留。

### M23：完整度、浏览器与文档收尾

> 状态：已完成（全量 verify、1280/1040/760 浏览器验收、文档与安全收尾）

#### 交付结果

- 全部新增能力纳入能力矩阵、README、ADR、PROGRESS 和归档/诊断说明。
- 完整 Definition of Done 与真实浏览器核心场景通过。
- 工作树干净，每个里程碑有独立可回滚提交。

#### 涉及模块

- 全仓测试、构建、sidecar/Tauri
- 文档、任务清单、发布与安全检查
- 浏览器验收

#### 前置依赖

- M18–M22 全部代码完成。

#### 实施步骤

1. 运行迁移、API、AI 任务、导出和 UI 专项测试。
2. 运行 `pnpm run verify`，修复而不是跳过任何失败。
3. 执行密钥/敏感日志、SQL 参数化、输入边界和导出内容审查。
4. 用真实浏览器完成创建到批准一集的主路径和多视口检查。
5. 更新测试数量、验收证据、遗留风险和能力状态。

#### 测试方式

- `pnpm run verify`。
- AI 漫剧专项 API/Web 测试。
- 1280/1040/760 浏览器主路径、控制台零 warning/error。
- `git diff --check`、仓库守卫和敏感内容扫描。

#### 验收标准

- 全部 lint、类型、测试、基准、构建、sidecar 和 Rust 检查通过。
- 核心场景实际验证，批准门、来源追踪和导出边界与 PRD 一致。
- PROGRESS 记录真实结果；未完成的图片/视频范围明确保留为后续阶段。

#### 风险与回滚方式

- 风险：全量回归暴露跨模块契约漂移。缓解：按失败根因修复并补回归，不降低门槛。
- 风险：浏览器状态依赖真实付费模型。缓解：开发验收使用已存候选或测试适配器，禁止产生未确认费用。
- 回滚：按 M18–M22 原子提交逐层回退；v22 数据先导出或恢复升级前备份。

### M24：四题材契约与无损兼容（已完成）

#### 交付结果

- Genre 增加东方玄幻和西方奇幻，项目创建、导入、查询、归档和恢复全部接受四题材。
- 旧字段保持物理兼容；新题材把年份/地点解释为故事纪年/起始地域。

#### 涉及模块

- Pydantic/TypeScript contracts、项目/稿件 API、归档语义校验和测试。

#### 前置依赖

- schema v22、归档 v10 和现有项目创建契约稳定。

#### 实施步骤

1. 扩展白名单枚举并集中定义题材元数据。
2. 为四题材创建、未知题材拒绝和新题材归档往返补回归。
3. 保持数据库和归档协议版本不变，证明无需迁移。

#### 测试方式与验收标准

- API/归档专项测试通过；旧数据零变化，新题材 round-trip 后题材与锚点一致。

#### 风险与回滚方式

- 风险：字符串分支遗漏导致错误显示。缓解：共享 label helper 与穷尽 Record。
- 回滚：移除新建入口但保留枚举读取能力，避免已创建项目变得不可恢复。

### M25：题材感知 AI、上下文与审校（已完成）

#### 交付结果

- 总导演、章纲、正文、字段重生成和示范模型理解四题材。
- 世界机制审校按题材检查重生因果、修炼体系或魔法/种族/阵营规则。
- 上下文和连续性提示使用通用作品/世界锚点文案。

#### 涉及模块

- AI gateway/prompts、fake model、ContextPacket、continuity、review。

#### 前置依赖

- M24 四题材契约。

#### 实施步骤

1. 扩展 Prompt 题材白名单与题材专项要求。
2. 对非重生题材禁止自动假设前世/未来知识，改用世界规则与代价约束。
3. 通用化上下文来源说明和审校标签，保留持久化 enum 兼容。

#### 测试方式与验收标准

- Prompt/假模型/导演专项回归；新题材候选具有题材场景且不强加重生设定。

#### 风险与回滚方式

- 风险：通用 Prompt 稀释原有重生专长。缓解：按 genre 明确列出不同检查维度。
- 回滚：Prompt 可独立回退，数据契约不受影响。

### M26：四题材 UI 与起航模板（已完成）

#### 交付结果

- 新建、旧稿导入、书架、工作区、拆书库、总导演和审校显示四题材正确文案。
- 增加东方玄幻/西方奇幻十章模板，保留历史/都市/现实锚点模板。
- 资料研究、时间线、知识账本与剧情沙盘切换为题材语境，并只展示当前题材可用模板。

#### 涉及模块

- `genre.ts`、CreateProjectForm、ManuscriptImportDialog、ProjectLibraryPage、WorkspaceShell、BookDirectorPanel、ReviewWorkbench、ResearchWorkbenchPage、NarrativeSandboxDialog、beta/sandbox templates。

#### 前置依赖

- M24/M25 契约和行为稳定。

#### 实施步骤

1. 建立共享题材标签和上下文文案 helper。
2. 接入所有创建、编辑和显示入口，修复旧二选一分支。
3. 新增两类起航模板和交互测试。

#### 测试方式与验收标准

- Vitest/pytest 模板测试；四题材均可创建，玄幻/奇幻不显示强制重生标签，760px 核心入口可达。

#### 风险与回滚方式

- 风险：四选项挤压窄屏。缓解：桌面两列、窄屏单列并真实浏览器验证。
- 回滚：共享 helper 和模板可独立回退，不删除项目数据。

### M27：完整验证与产品文档（已完成）

#### 交付结果

- README、PRD、能力矩阵、PROGRESS 和任务清单反映四题材真实能力。
- 完整 verify、安全检查和真实浏览器验收通过。

#### 涉及模块

- 全仓测试/build/desktop check、文档和浏览器。

#### 前置依赖

- M24–M26 完成。

#### 实施步骤

1. 运行专项和完整自动化，修复所有回归。
2. 检查输入白名单、SQL 参数化、Prompt 数据边界和归档兼容。
3. 在 1280/1040/760px 走查新建表单与工作区显示。
4. 更新测试基线、已交付能力和后续专用体系编辑器风险。

#### 测试方式与验收标准

- `pnpm run verify` 全绿；浏览器零 warning/error；工作树干净；不降低既有发布门。

#### 风险与回滚方式

- 风险：旧测试夹具暴露隐藏的二题材假设。缓解：修复业务分支而非批量改夹具掩盖。
- 回滚：按 M24–M26 原子提交回退 UI/Prompt；已产生新题材数据继续保持可读取。

## 10. 依赖关系与并行策略

必须串行的主路径：

~~~text
M0 工程治理
  → M1 可信度热修
  → M2 可恢复任务
  → M3 模型网关
  → M4 上下文编译
  → M5 全局拆书资产
  → M6 原创性门禁
  → M7 整书总导演
  → M8 审校与版本
  → M9 完整工作台
  → M10 发布硬化
  → M11 MVP
  → M12 剧情沙盘
  → M13–M17 作者智能扩展
  → M18–M23 AI 漫剧改编工作台
  → M24–M27 东方玄幻/西方奇幻题材扩展
~~~

两人及以上可以并行的工作：

- M2 后端任务引擎与前端任务中心可在契约冻结后并行。
- M3 provider adapter 与系统密钥 UI 可并行。
- M4 上下文编译器与黄金评测集制作可并行。
- M5 全局数据迁移与安全文件解析可并行。
- M6 规则检测器与蓝图编辑器可并行。
- M7 规划领域模型与创作交互可并行。
- M8 各审校维度可并行，但统一问题 schema 必须先冻结。
- M10 macOS 与 Windows 发布验证必须在各自原生环境并行执行。

禁止并行越过的安全门：

- 没有 M2，不扩展更多长任务 Agent。
- 没有 M4，不宣称长篇连续性可靠。
- 没有 M6，不允许参考蓝图自动进入整书规划和写章。
- 没有 M8 revision/change set，不开放多 Agent 自动改稿。
- 没有 M10 双平台真实验证，不宣称正式支持 Windows。

## 11. 测试与评测总策略

### 11.1 自动化测试层

1. 领域单元测试：状态机、事实、时间线、预算、原创性规则。
2. 仓储与迁移测试：逐版本升级、备份、失败回滚、归档往返。
3. provider 契约测试：假模型、录制回放、两个兼容端点。
4. API 集成测试：任务、取消、重试、权限边界、错误归一。
5. React 行为测试：保存门、任务中心、批准门、局部应用。
6. 真实浏览器 E2E：书架→规划→写章→审校→回灌→导出。
7. 桌面 E2E：sidecar、动态端口、令牌、退出回收、系统密钥。
8. 安装包测试：安装、首次启动、升级、卸载保留和恢复。

领域核心建议分支覆盖率不低于 90%；普通 UI 不追求统一数字，以关键路径和状态覆盖为准。每个缺陷必须先加入可复现回归测试。

### 11.2 固定黄金项目

- 历史重生 20 章：史料来源、制度约束、双时间线和未来知识失效。
- 都市重生 30 章：年代物件、行业逻辑、城市生活和资源成长。
- 都市现实锚点 15 章：地理、产业、交通、组织与年代资料回链。
- 都市商战 15 章：企业、媒体、消费者与监管反应，为沙盘预留。
- 拆书原创性集：两部合成同题材、一个近似变体、一部无关对照。
- 长篇规模集：100 章、30 万字正文、300 万字参考作品。

### 11.3 质量指标

- 数据可靠性：丢稿、错误覆盖、迁移损坏为 0。
- 任务可靠性：故障恢复后重复副作用为 0，成功块复用率 100%。
- 上下文质量：硬约束遗漏率 0，关键相关项召回率初始目标不低于 95%。
- 创作效率：从想法到完整章节候选的中位耗时和人工步骤持续下降。
- AI 可用性：章纲采用率、正文保留率、审校建议接受率可分版本比较。
- 原创性：近似集阻断率、无关集误报率和人工复核结论可追踪。
- 成本：每章和每百万参考字的调用数、token、费用与重试浪费可见。

## 12. MVP 发布门

只有同时满足以下条件才能把版本称为“网络文学创作 AI 工作台 MVP”：

- 作品、正文、版本、事实、参考资产和任务可在重启后恢复。
- 作者从一句创意可得到前三章计划和完整首章候选。
- 30 万字项目上下文、编辑和检索达到性能目标。
- 300 万字参考作品可分段、缓存、恢复并跨项目复用。
- 高风险参考方案无法直接进入规划或写章。
- 七维审校可独立运行，所有问题带证据。
- 任何 AI 结果未经批准不能写正文或正式事实。
- API Key 使用系统密钥库，日志、归档和诊断包不泄漏。
- macOS 和 Windows 干净机安装、升级、卸载和数据保留通过。
- 没有 P0/P1 数据安全、丢稿、安装、原创性或权限边界缺陷。
- 真实作者连续完成至少 10 章核心流程。

## 13. 全局风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 模型质量波动 | 输出不可比较 | prompt、context、provider、model 和评测集全版本化 |
| 长篇事实漂移 | 后期剧情失真 | 结构化 Canon、ContextPacket、来源、批准门和黄金集 |
| AI 成本不可控 | 用户停止使用 | 调用前估算、分块缓存、按 Agent 选模、取消和限额 |
| SQLite 并发锁 | 任务和保存互相阻塞 | 单写者、短事务、WAL、租约和故障测试 |
| 参考作品版权与隐私 | 法律和信任风险 | 权利声明、原文隔离、外发预览、删除验证和原创性门禁 |
| 原创性误报/漏报 | 阻碍创作或错误放行 | 多信号、分级证据、合成基准、版本化阈值和人工评审 |
| MiroFish 许可污染 | 商业化受限 | 只参考功能，清洁实现，ADR 和依赖/代码来源审查 |
| Tauri 加 Python 打包复杂 | 双平台延期 | 原生 CI、早期 Windows runner、固定锁文件和干净机测试 |
| 文档再次漂移 | 重复开发与错误判断 | 每个里程碑完成时同步能力矩阵和验收证据 |
| 过度自动化削弱控制 | 作者不信任 | 可见上下文、候选、锁定、diff、批准门和逐阶段取消 |

## 14. 计划审批后的第一批工作

PLAN.md 获批后，只启动 M0 和 M1，不直接跳到新 Agent 功能。第一批提交应按以下顺序：

1. 建立版本控制、CI 和文档状态基线。
2. 用回归测试固定“最新事实遗漏”和“1040 像素 AI 不可达”两个问题。
3. 修复这两个问题并建立显式数据库迁移框架。
4. 提交 M2 任务模型 ADR 和最小垂直切片规格。
5. 经里程碑验收后自动进入 M2，不再为每个后续里程碑重复请求执行授权；只有本计划第 6 节的产品决策、权限扩张或破坏性数据操作需要再次确认。

## 15. 2026-09-11 审查后的核心闭环计划（M28–M36）

### 15.1 目标与实施边界

本批次以 `docs/product/ai-first-author-workflow-spec.md` 为产品与工程规格，把现有功能岛收敛为：

```text
人工选题 → 多书模式配方 → AI 原创迁移 → AI 完整单章候选
        → 人工局部调整与批准 → 事实/偏好回流 → 下一章
```

实施继续遵守以下边界：macOS 首发、OpenAI-compatible 优先、模型外发与费用先确认、AI 只写候选、参考原文隔离、高风险原创性结果不可绕过、不做无人值守多章或自动发布。

### 15.2 里程碑依赖

```text
M28 稿件安全与参考恢复
  → M29 人工选题台
  → M30 长篇写作模式 v2
  → M31 多书配方与原创迁移
  → M32 统一创作上下文与计划重基
  → M33 单章生产与候选调整工作台
  → M34 定稿事实与作者偏好回流
  → M35 连续作者工作台与下一章
  → M36 四题材质量、E2E 与封闭测试门
```

M30 的后端模式资产与 M29 的前端选题台可在各自契约冻结后并行；M31 之后必须按顺序实施，避免配方、上下文、候选和回流出现版本旁路。

### M28：稿件安全与参考模式恢复能力

> 状态：2026-09-11 已完成；自动化与真实浏览器验收记录见 `PROGRESS.md`。

#### 交付结果

- 任何旧 AI 候选都不能覆盖生成后出现的人工修改或已批准正文。
- 参考模式应用具有 draft/active/archived 生命周期；失败或高风险应用可以停用，不再永久阻断全项目 AI。
- 所有采用入口显示可恢复提示，并提供“查看差异 / 重新生成”而不是静默覆盖。

#### 涉及模块

- `GenerationRun`、章节仓储与采用 API。
- Reference application schema、门禁查询、项目归档。
- DirectorPanel、TaskCenter、ReferenceLabPanel 与共享契约。

#### 前置依赖

- 现有 revision、GenerationRun、原创性门禁和 schema v22 稳定。
- 不需要真实模型或外部费用。

#### 实施步骤

1. 先增加“run 基于 r0、作者保存 r1、调用方传 r1”回归测试，证明当前缺陷。
2. 将采用操作改为单事务校验 `chapter.revision == request.revision == run.expected_chapter_revision`，并禁止对已批准章节写入。
3. 增加 reference application 生命周期迁移、模型、仓储和归档映射。
4. 门禁只读取当前激活且本次上下文会使用的应用；停用保留历史、报告和来源。
5. UI 增加过期候选解释、重新生成入口，以及失败模式“去检查 / 暂停使用”操作。

#### 测试方式

- API：旧候选、并发采用、已批准章节、重复采用、事务无副作用。
- 迁移/归档：v22 升级、重复启动、active/archived 往返和旧归档默认值。
- Web：Director 与任务中心过期候选；参考应用停用后 AI 恢复。
- 专项通过后运行 `pnpm run verify`。

#### 验收标准

- 旧 run 即使携带当前 revision 也返回冲突；正文、版本、候选状态均不变化。
- 已批准章节不能被候选采用接口降级或覆盖。
- 高风险应用停用后，不参与上下文和门禁；重新激活必须重新满足当前原创性检查。
- 全量验证通过且没有删除或放宽现有测试。

#### 风险与回滚方式

- 风险：旧前端无法识别新的冲突码。缓解：保持 HTTP 409，增加稳定错误 code 和兼容 message。
- 风险：新生命周期改变旧应用默认行为。缓解：迁移统一设为 active，行为与升级前一致。
- 回滚：先隐藏生命周期 UI，再回退 API；数据库列保持向前兼容，不删除历史数据。

### M29：AI-first 人工选题台

> 状态：2026-09-11 已完成；schema v24、归档 v12、全量自动化与真实浏览器闭环见 `PROGRESS.md`。

#### 交付结果

- 新项目默认进入选题台，而不是空白正文。
- 作者可保存选题草稿、比较 3 个 AI 候选、锁定字段、记录拒绝原因并确认选题 revision。
- 模板中的创意、现实/世界锚点和前十章目标真正进入选题与总导演上下文。

#### 涉及模块

- TopicDecision 数据模型、迁移、仓储、API、归档与共享契约。
- CreateProjectForm、BookDirectorPanel、项目启动路由和起航模板。
- Director startup prompt 与 ContextPacket 来源。

#### 前置依赖

- M28 版本门稳定。
- 现有 BookBlueprint 候选、FieldLock 和 Director Job 可复用。

#### 实施步骤

1. 建立版本化 TopicDecision：平台/读者、细分题材、命题、核心欲望、长期与前三章承诺、限制和参考用途。
2. 新建项目时持久化模板默认值与作者输入；不再只填充本地组件状态。
3. AI 候选改为并排决策矩阵，支持逐字段锁定、局部重生成和拒绝原因。
4. 明确“确认选题”动作；确认或修订后标记已有蓝图及下游计划待复核。
5. 项目恢复时进入服务端给出的结构化 next_action。

#### 测试方式

- 数据库/归档/API：revision、锁定、非法输入、旧项目默认行为。
- Prompt：选题字段与模板信息确实进入 startup，上下文不包含未确认候选。
- Web：新建→比较→编辑→确认→恢复；刷新后决定不丢失。
- 1280/1040/760 浏览器和键盘验收。

#### 验收标准

- 作者不调用 AI 也能确认完整选题；AI 方案始终是候选。
- 新作者不填写底层蓝图 schema 即可进入整书策划。
- 未确认或被拒绝的字段不影响后续模型；锁定字段不被局部重生成覆盖。
- 修改已确认选题会产生新 revision 并明确显示影响范围。

#### 风险与回滚方式

- 风险：新建流程变长。缓解：提供“只填一句话，稍后完善”，但仍需确认关键字段。
- 风险：旧项目没有 TopicDecision。缓解：按已有项目和蓝图生成可编辑草稿，不伪造已确认状态。
- 回滚：保留原创建参数和写作入口；新表为附加数据，可关闭新选题台而不影响正文。

### M30：长篇拆解与写作模式 v2

#### 交付结果

- 单本作品可独立分析；300 万字作品默认生成约 6 张可复用的 50 万字阶段卡。
- 形成“阶段卡 → 单书演变卡 → 多书融合素材”三级资产。
- 六维故事骨架之外，新增带证据的网文钩子、兑现节奏、情绪、场景、POV 和表达参数。
- 拆解前可见调用数、缓存命中、token 和费用估算。

#### 涉及模块

- Reference 分段、Map/Reduce Job、analysis cache、pattern models、全局资产库。
- Provider prompt/structured output、来源谱系和解析安全边界。
- Reference Library UI、导入组件、契约和归档。

#### 前置依赖

- M28 reference application 生命周期。
- 现有约 5 万字 Map、50 万字 segment、Job/Artifact 和全局 ReferenceWork。

#### 实施步骤

1. 定义 CraftPattern v2 schema 与证据格式：book/segment/chapter/span/evidence digest/confidence。
2. 允许单书分析并持久化阶段卡；移除“至少两本”作为分析前置，仅在多书融合时检查来源数。
3. 生成单书阶段演变卡，并允许从多次任务中选择阶段卡二次融合。
4. 扩展结构化 prompt 和严格解析；证据必须落在实际输入区间，错误锚点使任务失败而非静默接受。
5. 增加调用前估算与缓存预览，统一项目内和全局库的 TXT/MD/PDF/DOCX/EPUB 导入能力与 25MB 文案。
6. 升级模式库 UI 为可导航的“来源 → 阶段卡 → 单书演变 → 配方素材”。

#### 测试方式

- 3 万、50 万、300 万字确定性夹具；单书、跨任务二次融合和中断恢复。
- 证据越界、伪造章节、结构化输出缺字段、超限和压缩包攻击测试。
- 费用估算与实际任务块数一致；缓存命中不重复调用。
- Web 分步流程、证据侧栏、导入一致性及真实浏览器长页面验收。

#### 验收标准

- 300 万字作品可得到约 6 张独立阶段卡，任一卡可浏览、停用、复用或加入配方。
- 每项技法都有合法来源锚点和置信度；正文服务无法读取参考原文。
- 单书分析不再要求伪造第二本来源；多书融合仍要求至少两个不同作品。
- 提交长任务前作者看到预计调用范围和费用。

#### 风险与回滚方式

- 风险：v2 输出使模型结构化失败率上升。缓解：版本化 schema、字段级重试和 v1 只读兼容。
- 风险：证据摘要泄露长段原文。缓解：字符上限、哈希、净化和正文上下文隔离测试。
- 回滚：保留 v1 卡片读取；v2 通过版本字段旁路存储，可关闭生成入口但不删除产物。

### M31：多书模式配方与 AI 原创迁移

#### 交付结果

- 模式卡可跨项目复用，项目只引用不可变版本快照。
- 作者可指定“甲学开篇、乙学升级、丙作反例”、权重、适用阶段和 preserve/transform/avoid。
- AI 根据当前选题生成 3 套原创迁移方案，人物关系默认重构。
- 原创性门禁绑定聚合配方和最终 BookBlueprint revision。

#### 涉及模块

- WritingPatternRecipe、RecipeSource、AdaptationProposal、版本指纹与全局模式库。
- PatternAdaptation provider task、OriginalityGuard、scene/plot graph。
- 配方与迁移工作台、蓝图比较和应用 API。

#### 前置依赖

- M29 已确认 TopicDecision。
- M30 CraftPattern v2 及证据资产。

#### 实施步骤

1. 建立全局模式卡/配方与项目快照；每个维度保存来源、权重、用途、阶段和冲突决定。
2. 实现确定性 recipe compiler，输出冲突、禁止复刻项和来源 revision 指纹。
3. 新增有费用预览的 AI 原创迁移任务，产生 3 套结构差异明确的蓝图候选。
4. 对来源专名、人物组合、独特节拍和多卡组合风险执行门禁。
5. 作者采用后再对实际 BookBlueprint revision 运行门禁；报告与哈希不匹配时禁止规划或写章。
6. 提供全局复用、复制为新版本、停用和归档操作。

#### 测试方式

- 1–5 本书、不同权重/阶段、冲突规则和稳定编译顺序测试。
- 模型输出专名、相同人物组合或独特场景序列时必须阻断。
- 改变选题或配方后，旧蓝图报告失效；锁定字段始终保持。
- Web：来源矩阵→三方案比较→修改→门禁→采用完整旅程。

#### 验收标准

- 配方不是多个卡片的无序拼接；相同输入得到稳定 profile fingerprint。
- 三套方案在核心冲突、人物关系或场景组织上至少有明确结构差异。
- 高风险组合不能通过拆成多张应用规避检查。
- 新项目可引用既有模式卡而无需重新拆解原文。

#### 风险与回滚方式

- 风险：自动原创迁移给出表面换名。缓解：结构差异断言、组合场景图检测和人工比较。
- 风险：全局卡片修改影响既有作品。缓解：项目引用不可变 revision 快照，新版本显式升级。
- 回滚：禁用 Adaptation Adapter，保留人工创建配方与既有 v1 application 路径只读。

### M32：统一 CreativeContext 与计划重基

#### 交付结果

- startup、expansion、field、brief、draft、candidate-review 和 canon-reconciliation 使用同一 CreativeContext Interface。
- 当前 WritingPatternProfile 在 8K/16K/24K 预算下均不会被静默丢弃。
- 选题或配方 revision 变化会标记 BookBlueprint、VolumePlan 和 RollingPlan 待复核，并提供影响预览与重基。

#### 涉及模块

- ContextCompiler、Director startup/expansion/field context、Review context。
- BookBlueprint/VolumePlan/RollingPlan 版本依赖与 stale 状态。
- 影响预览、重基 API 和 UI。

#### 前置依赖

- M31 recipe/profile fingerprint 稳定。
- 现有 ContextPacket、FieldLock 和计划版本模型。

#### 实施步骤

1. 抽取 CreativeContext purpose 接口，并先适配现有章节生成。
2. 将 compact WritingPatternProfile 标为 required current-state 约束；只携带抽象规则。
3. 依次迁移 Director startup、卷纲扩展、字段重生成和 Review，删除各自临时 JSON 拼装旁路。
4. 为计划保存 topic/profile/base-blueprint 指纹，依赖变化时标记 stale。
5. 增加“保持作者锁定字段”的影响预览与重基候选，仍由作者采用。

#### 测试方式

- 8K/16K/24K 预算；2/5/10 张模式卡；确定性排序和 packet hash。
- startup/field/draft/review 使用相同 profile fingerprint。
- 修改配方后旧 packet、计划和原创性报告失效；重新采用后恢复。
- 原始参考文本和未确认偏好在所有 purpose 下均不存在。

#### 验收标准

- 删除某一 UI 入口不会改变上下文规则；所有生成通过同一 Interface。
- 已开书后再应用配方不会与旧蓝图静默并存，作者可见受影响字段。
- 重基只产生候选，不覆盖锁定字段或已批准正文。

#### 风险与回滚方式

- 风险：上下文重构改变现有 Prompt 行为。缓解：快照黄金测试和 purpose 逐个切换。
- 风险：required profile 挤占正文事实。缓解：紧凑编译、单独预算和超限显式失败。
- 回滚：保留旧 compiler Adapter 一版，通过功能开关按 purpose 回退。

### M33：ChapterProduction 与候选调整工作台

#### 交付结果

- 一个深 ChapterProduction Module 统一章纲、写前检查、正文候选、候选审校、采用/拒绝和 WritingOutcome。
- 候选可以左右对照、编辑、局部采用、局部重生成、锁定、撤销和多候选合并。
- Director、AI 共创、任务中心和示范模型不再形成相互矛盾的正文入口。

#### 涉及模块

- Chapter Job、GenerationRun/ChapterVersion/TextChangeSet、Review Orchestrator。
- ModelRoutingPolicy 与明确的 outline/draft/rewrite/review Job kind。
- 主编辑器、候选对照器、局部命令与版本历史。

#### 前置依赖

- M28 revision 安全门。
- M32 CreativeContext 与计划状态可靠。

#### 实施步骤

1. 建立 ChapterProduction use-case Interface 和 WritingOutcome 追踪链路。
2. 将真实异步章稿流程接入；示范模型降为同一接口后的 Demo Adapter，停用同步旁路入口。
3. 让写前检查真正校验读者承诺、钩子、状态变化、情绪兑现和章末悬念；失败时不调用正文模型。
4. 在候选未采用时运行七维审校，并将结果与候选 revision 绑定。
5. 实现候选可编辑副本、选区 AI 指令、锁定范围和 TextChangeSet 局部应用。
6. 所有整章/局部采用共用 M28 的原子 revision guard；GET 读取不再物化新候选。

#### 测试方式

- 状态机、幂等、重启恢复、取消、重复 GET、模型失败和费用记录。
- 写前缺字段时确认正文 Adapter 未调用；候选审校不读取正式正文旁路。
- 整章/局部/多候选采用冲突、锁定范围、撤销和历史版本测试。
- Web 键盘、长正文、1280/1040/760 对照工作台与控制台验收。

#### 验收标准

- 作者只看到一个“生成本章候选”主入口，完成结果始终停在候选区。
- 旧候选、旧审校报告和旧变更集均不能写入新 revision。
- 作者能在不手工重写整章的情况下完成局部调整和批准。
- 每次生成可追溯 ContextPacket、Prompt、Adapter、质量报告和最终决定。

#### 风险与回滚方式

- 风险：正文入口合并影响已有用户习惯。缓解：旧入口先跳转到新工作台并保留一版兼容提示。
- 风险：局部应用产生字符偏移。缓解：证据哈希、上下文锚点和冲突时拒绝，不猜测替换位置。
- 回滚：新工作台通过功能开关关闭；底层版本与候选记录保持可读。

### M34：定稿 Canon 与作者偏好反馈闭环

#### 交付结果

- 章节批准后自动从最终正文生成带证据的 typed Canon delta 候选。
- AI 候选与最终稿差异可生成作者偏好候选，作者确认后影响后续章节。
- 确认事实后更新人物、关系、资源、地点、知识、伏笔、境界/能力和双时间线，并自动补滚动计划。

#### 涉及模块

- CanonReconciliation、AuthorAdjustmentMemory、FactChangeSet 和账本仓储。
- 批准事务后的可恢复任务、ContextCompiler 项目偏好输入。
- 事实/偏好确认 UI 与 WritingOutcome。

#### 前置依赖

- M33 最终章节版本与 WritingOutcome 完整。
- M32 CreativeContext purpose 支持 reconciliation 和 preference。

#### 实施步骤

1. 扩展 typed delta schema 与正文证据，保留旧 FactKind 只读兼容。
2. 章节批准后幂等创建 reconciliation Job，以最终正文版本而非章纲作为事实依据。
3. 作者批量接受/编辑/拒绝，单事务更新多个正式账本；重复采用无副作用。
4. 从 WritingOutcome 差异生成压缩偏好候选，限定项目/题材/单章作用域和冲突提示。
5. 只有已确认偏好进入 CreativeContext；拒绝或删除立即使后续 packet 指纹变化。
6. Canon 确认后自动补齐最远 3–5 章滚动计划候选。

#### 测试方式

- 正文与章纲冲突、证据越界、重复运行、部分接受和多账本原子回滚。
- 未确认偏好隔离、删除/拒绝、锁定蓝图冲突和上下文预算。
- 东方玄幻境界/资源代价、西方奇幻魔法/阵营、两类重生知识失效黄金样本。
- Web：批准→事实确认→偏好确认→下一章上下文完整旅程。

#### 验收标准

- 正文中新事实可追到精确证据，拒绝项不进入任何正式账本。
- 同一最终版本重复运行结果幂等；多账本失败时全部回滚。
- AI 后续可读取作者确认的抽象偏好，但不读取大段历史 diff。
- 章纲改变后事实仍以最终正文为准并向作者提示差异。

#### 风险与回滚方式

- 风险：模型误提取污染 Canon。缓解：候选隔离、证据校验、类型白名单和人工批准。
- 风险：偏好过拟合少量修改。缓解：置信度、作用域、出现次数和作者显式确认。
- 回滚：停止自动创建任务，保留候选记录；正式事实变更仍可通过版本/恢复点回退。

### M35：连续作者工作台与下一章动作

#### 交付结果

- 工作台按“选题与整书 / 当前章计划 / AI 候选 / 审校定稿”组织，当前阶段和唯一下一步始终可见。
- 账本、资料、拆书、研究和作者工具进入辅助抽屉或独立页面，不与本章主操作争夺首屏。
- 定稿后主 CTA 进入下一待写章节，次操作查看上次定稿；刷新和返回键保持当前项目、页面和阶段。

#### 涉及模块

- WorkspaceShell、DirectorPanel、BookDirectorPanel、SerialControlPanel、App 路由与样式。
- 服务端 resume_card/next_action。
- 模态焦点管理、skip link 和响应式导航。

#### 前置依赖

- M29–M34 的状态与动作契约稳定。

#### 实施步骤

1. 服务端返回结构化 next_action，不再固定恢复到最后一个已有进展章。
2. 将右侧超长面板改为四阶段导航和固定下一步操作；保留正文为中央主工作面。
3. 批准、Canon/偏好确认和滚动计划完成后更新下一步状态。
4. 将 active view/stage 写入 URL；刷新、前进后退和深链接恢复正确位置。
5. 统一导入入口与题材文案；修复窄屏错误短标签、过小字号和正文 focus ring。
6. 补模态 focus trap、背景 inert、关闭后焦点恢复和跳到正文/本章 AI 的 skip link。

#### 测试方式

- 结构化 next_action 的未开书、待写、草稿、待审、定稿和项目完成状态测试。
- Web 黄金旅程、URL 恢复、键盘仅操作、焦点循环和无障碍名称测试。
- 1280×720、1040×900、760×900 真实浏览器；无横向溢出和控制台错误。
- 性能对比首屏请求数、交互延迟和长面板高度。

#### 验收标准

- 新项目首屏是选题，写作项目首屏直接落在服务端建议的当前任务。
- 任一阶段首屏只有一个主 CTA；核心动作不需要在约 6 个视口的右栏中寻找。
- 已定稿章节不会阻止进入计划中的下一章。
- 键盘焦点始终可见，弹窗不会把焦点泄漏到背景。

#### 风险与回滚方式

- 风险：信息架构改动影响熟悉旧页面的用户。缓解：保留辅助入口和“经典布局”短期功能开关。
- 风险：URL 状态与未保存正文冲突。缓解：所有导航继续先执行现有 flush 保存门。
- 回滚：路由与阶段外壳可独立回退，后端 next_action 为加法字段。

### M36：四题材质量、完整 E2E 与封闭测试门

#### 交付结果

- 四题材各有确定性黄金项目，覆盖选题、模式、正文、调整、回流和下一章。
- 东方玄幻/西方奇幻的场景图与原创性语义覆盖升级阶梯、能力代价、宗门/种族/阵营和通用母题。
- 完整工程验证、浏览器、桌面、安全、迁移和归档全部通过。
- 形成 5–10 位作者、每人至少 10 章的封闭测试协议与数据报告入口。

#### 涉及模块

- 原创性规则与黄金集、E2E fixtures、beta metrics、诊断与文档。
- 全仓 lint/type/test/build/benchmark/desktop/release preflight。

#### 前置依赖

- M28–M35 全部完成。
- 真实作者验收需要外部参与者；真实模型测试需要明确费用授权。

#### 实施步骤

1. 为历史重生、都市重生、东方玄幻和西方奇幻各建立一条无费用黄金旅程。
2. 标定玄幻/奇幻场景图阈值，确保常见母题不误报、独特组合能阻断。
3. 增加跨模块恢复旅程：旧候选、失效配方、任务重启、事实回滚和下一章恢复。
4. 运行安全清单：输入白名单、参数化 SQL、上传/ZIP 防护、密钥与日志扫描、loopback token。
5. 运行完整 `pnpm run verify`、真实浏览器与 macOS 桌面冒烟；更新 PLAN、PROGRESS、README 和能力矩阵。
6. 执行或准备封闭测试：记录候选采用率、正文保留率、人工调整类型、连续十章完成率和主观质量，不用聚合点击数冒充文学质量。

#### 测试方式

- 四题材 API/Web/Context/Prompt/原创性黄金测试。
- `pnpm run verify`、`pnpm run release:preflight`、干净目录安装和 macOS 桌面冒烟。
- 1280/1040/760 浏览器完整闭环，控制台 warning/error 为 0。
- 5–10 位作者每人至少 10 章盲测；人工评分与系统指标分开记录。

#### 验收标准

- 自动化与工程发布门全部通过，没有 P0/P1 数据安全、门禁或闭环缺陷。
- 四题材都能完成“选题→配方→AI主写→人工调整→回流→下一章”。
- 参考模式结论 100% 有来源锚点，参考原文 0 次进入正文上下文。
- 只有真实作者测试完成后，才把“作者验证”标记完成；缺少外部参与者时必须保留为明确遗留项。

#### 风险与回滚方式

- 风险：自动化全绿但文学质量不达标。缓解：真实作者盲测、保留率与逐章访谈，不降低发布门。
- 风险：真实模型费用或参与者不可用。缓解：先完成零费用工程门和测试协议，状态标记外部待验收。
- 回滚：按 M28–M35 原子里程碑回退功能；数据库使用向前兼容迁移和升级前备份，不降级覆盖用户数据。

### 15.3 本批次 Definition of Done

只有以下条件同时满足，M28–M36 才能整体宣称完成：

1. 每个里程碑的专项测试、全量验证与浏览器验收均记录在 `PROGRESS.md`。
2. 旧候选、旧配方、旧原创性报告或旧变更集均不能覆盖/污染较新正文与正式 Canon。
3. 项目可以完成一次不调用付费模型的确定性黄金闭环。
4. 所有新表完成旧库升级、失败回滚、归档往返和诊断验证。
5. `pnpm run verify`、release preflight 和 macOS 桌面冒烟通过。
6. 文档区分“工程闭环完成”和“真实作者验证完成”，不得混报。
